# THREADS.md — Living Dev Thread Tracker

**Version:** 9.55
**Date:** 2026-07-31
**Scope:** Culture Engine monorepo (Dissonance + Don't Turn Around)

> **Documentation-system reconciliation (v9.55):** `DISSONANCE-DOCSYSTEM.md` now
> governs document authority. `SYSTEMS.md`, `DECISIONS.md`, and
> `OPEN-QUESTIONS.md` are canonical registries. Files under the legacy `plans/`
> folder are classified by `engineering/PLAN-RECONCILIATION.md` and are not
> canonical merely because they exist. New canonical design summaries live under
> `design/`. Historical prose below remains useful audit history but must be read
> through those registries.

> **North star:** Audio is the key element of this game. Sound and music are not a subsystem — they are a first-class simulated world force woven through every system: ecology, bodies, behavior, institutions, identity, navigation, threat, and play. Every thread below either produces sound, consumes it, or is shaped by it. When in doubt, decide in favor of the dream: a world you learn by listening.

**Note:** v9.22 is a full docs/-directory pass (Dan: "read all the docs, organize, and update THREADS.md to reflect the current state"). Verified every claim in this file against actual git history and source, not just prior session notes. Found: five new `docs/plans/*.md` handoff docs sitting uncommitted since last pass, never reconciled into the registry (T13 boulevard analysis, creature-silhouette direction, the lat/long-grid prompt, and two competing T7-substrate placement designs) — all folded in below. Also found real, undocumented progress: the T22 hero-tree POC didn't just land, it shipped five real Poly Haven `.glb` assets with a solved multi-material thin-instancing gotcha; the lat/long graticule overlay shipped, closing a NOTES.md parking-lot item; a stuck-movement-key-on-blur fix landed with no THREADS entry at all. New **"Doc inventory"** section added — several "owning docs" this file has pointed to for months (`dta-session-notes.md`, `acoustic-world-systems-prompt-v1.md`, `pursuer-embodiment-prompt-v1.md`, others) do not exist as committed files anywhere in the repo; this was previously known only for T3's v3 prompt, now confirmed systemic and listed exhaustively so future sessions stop assuming they can just go read them. v9.5 merges the local trail-viewer session per its discrepancy report (a parallel-session overwrite raced with and dropped that session's commit — reconciled per the single-writer rule below): P8 corrected — trail-viewer is the successor foundation, not a museum piece; T21 expanded with the traversal/atmosphere layer + Copy/Load View spec; O8/T22 staleness fixed to match. v9 itself wove `acoustic-world-systems-prompt-v1.md` (fiction→design→implementation spine) through existing threads; four conflicts surfaced as O6–O8 + oscilloscope supersession — not silently resolved.

---

## Thread ID registry (reconciled v9.3 — IDs are now stable; do not renumber)

| ID | Thread | State |
|---|---|---|
| T1/T2 | Environment profiles / Dev HUD (profile pattern + authoring loop) | active |
| T3 | Pursuer extraction | partially landed (A/B done pre-existing; C deliberately deferred) — see entry for open sign-offs |
| T4 | Pursuer embodiment / profile system | blocked on T3 |
| T5 | Reactive environment seam | parked — shares transition seam with T23 |
| T6 | Landscape profiles (targets list) | parked — targets become T23 regions |
| T7 | World population / window occupancy | parked — reframed as world assembly under T23; placement-substrate question resolved 2026-07-27 (see T23 note) |
| T8 | — retired: merged into T17 (asset queue) | merged |
| T9 | Sound-as-control → voice rig | horizon |
| T10 | Lore / AGENTS.md (contains T-Lola, T14, T15, T16) | parallel |
| T11 | Provenance / multiplayer | parked (reserved ID) |
| T12 | Mobile control layer | queued |
| T13 | Godot PoC excavation | stills captured, analysis pass written up (`t13-boulevard-extraction-v1.md`); video pass of patrol loop outstanding |
| T14 | Viewpoint binding / possession | lore-committed, impl. aspirational |
| T15 | DTA narrative spine | provisional (lore) |
| T16 | Single-level hub structure | provisional (lore) |
| T17 | Character rigging learning track | active |
| T18 | Throw & distraction system | handoff ready |
| T19 | Wildlife / fauna | handoff ready |
| T20 | Acoustic world systems (master spine) | owning doc ready |
| T21 | Geo pipeline / TrailViewer — successor foundation | active; traversal/atmosphere layer landed; O1–O3 gate geo next steps |
| T22 | DTA forest visual & play-scenario direction | direction set |
| T23 | Region system / continuous world | design committed |
| T24 | Forest performance & chunking | handoff ready — parallel-safe now |
| T25 | Atmosphere grading + time-of-day | precipitation/storm foundation landed 2026-07-30; grading compose seam still design-only |
| T26 | Identified-overlay landmark layer (stable per-instance identity) | new 2026-07-27 — salvaged from parked instance-placement-prompt-v1.md, scoped down |
| T27 | Zone-field atmosphere blending | new 2026-07-27 — salvaged from parked instance-placement-prompt-v1.md, scoped down |
| T28 | Rural infrastructure backlog (silos, windmills, water towers, self-storage, airport, highway, compass) | new 2026-07-27 — backlog only; power lines already shipped, see T21 |
| T29 | Diegetic communications / terminal layer | experimental — offline docking slice first; networking remains T11 |
| T30 | Instanced material pipeline (scatter-prop variation + emissive data patterns) | first pass landed 2026-07-29 — see entry for what's deferred |
| T31 | Emitter / captured patrol drone (interference verb) | acquisition v1 complete; interference/control v2 gated; live QA follow-up deferred |
| T32 | Repair-pipeline hack (ride-the-mend) | experimental; downstream of T31 |
| T33 | Infrastructure corridor (rail + power + EM charge) | provisional; Phase 0 sign-off required |
| T34 | Acoustic mech disable (“the right song”) | experimental; blocked on T9 and T4 |
| T35 | Underground network (parent; contains Milo's workshop) | first node/corridor + inventory-as-room presentation landed; live visual review pending and tuning deferred |
| T36 | Rey Caverns + gatekeepers (inhabited pole) | boundary teaser landed; inhabited space and trust economy gated |
| T37 | Milo's apartment archaeology room | queued; architecture approved, Blender pipeline and room implementation outstanding |

---

## Doc inventory (verified 2026-07-22; paths refreshed 2026-07-28 — see the folder reorg note below)

**Later addition (2026-07-24):** `plans/dissonance-lineglass-engineering-review-prompt.md` is present and is summarized under T21; it is a prospective Lineglass implementation/review artifact, not an existing runtime system.

**Underground/interference fold-in (2026-07-31):** the tracked owning set now includes `dissonance/story-manifest.md`, `dissonance/world-save-contract.md`, `dissonance/underground-network-concept-v1.md`, `dissonance/rey-caverns-concept-v1.md`, and `todo/emitter-drone-prompt-v1.md` (executed acquisition scaffold, retained for live QA follow-up and v2 scope). The source delta and its obsolete cluster handoff now live under `archive/` as v9.53 provenance rather than competing executable trackers. The archived handoff's proposed sonic-semantics T34 was not registered because stable T34 is Acoustic mech disable. Tonal-language work remains parked on the unadapted Dissonance Audio HUD repo; no replacement thread is opened here.

**Folder reorg (2026-07-28, Dan: "can you reorganize the docs folder for me"):** extended the `dissonance/`/`dta/` per-game split Dan had already started by hand (2 files moved) to the rest of the tree — moves only, no content rewrites, done via `git mv` to preserve history. `trail-viewer-poc/` → `dissonance/world/` (matches the `apps/world` rename); `game-story-and-trails-plan.md` → `dissonance/`; `monorepo-docs/` → `monorepo/`; `generation-systems-audit.md` → `monorepo/` (it audits both games' systems plus shared packages, not one game); `THREADS-fold-in.260721.md` and `THREADS-v9.20-addendum.md` (both superseded, kept only for provenance/citation) → a new `archive/`. `plans/` deliberately left flat and unsplit — it's cross-referenced by path from dozens of places in this file's own prose, and splitting it by game would have meant editing most of them for uncertain navigability benefit. Every broken cross-reference this caused was found by grepping the whole repo (not just `docs/`) and fixed: `apps/world/README.md`, `apps/museum/dont-turn-around/README.md`, `apps/world/src/world/HeroTreeInstances.ts`'s source comment, `plans/addendum-reconciliation-implementation-v1.md`, and this file's own Doc Inventory entry + two `THREADS-fold-in` citations (T22 section) + one `monorepo-docs` citation (T3 section). `docs/README.md` rewritten to match. Paths below updated in place rather than left stale, same as the FoliageSwayPlugin correction earlier in this section.

This file frequently names a handoff/owning doc as though it's a committed file. It often isn't — the doc existed only inside a web session's output and was never saved to the repo. This has been called out once before (T3's `pursuer-extraction-prompt-v3.md`); this pass checked every such reference against the actual filesystem. Treat any name below marked **missing** as unusable until someone commits it — don't assume a future session can "just go read it."

**Present in `docs/`** (paths relative to `docs/`): `THREADS.md`, `archive/THREADS-fold-in.260721.md` (content already landed, see T22 — kept because `HeroTreeInstances.ts` cites it by name in a source comment), `NOTES.md`, `README.md`, `monorepo/generation-systems-audit.md`, `dissonance/dissonance-forest-graphics-prompt.md`, `dissonance/game-story-and-trails-plan.md`, `dta/notes for don't turn around.md`, `monorepo/260615 Monorepo Prompt.md`, `monorepo/pursuer-extraction-prompt.md`, `monorepo/pursuer-extraction-continuation.md`, `dissonance/world/{README,architecture,data-pipeline,park-map-overlay,scale-tuning}.md`, `plans/dissonance-trail-data-poc-prompt.md`, `plans/creature-silhouette-direction-v1.md`, `plans/geo-grid-engineering-prompt.md`, `plans/instance-placement-prompt-v1.md`, `plans/scatter-placement-prompt-v1.md`, `plans/sound-as-control-prompt-v1.md`, `plans/t13-boulevard-extraction-v1.md` (an accidental byte-identical duplicate, `t13-boulevard-extraction-v1 (1).md`, was found alongside it and deleted this pass), `plans/animals.webp` (crystal-construct mech-animal reference sheet — pairs with `creature-silhouette-direction-v1.md`), `plans/smr-shed.webp` (on-trail photo of a fenced stone building with a chimney — filename confirms it as SMR shed reference material, i.e. T22's "Shed / comfort station" feature, though the pictured building has an intact shingled roof and chain-link fence rather than photo 10's described corrugated roof and undergrowth-swallowed state — possibly a different angle or a second shed on the property; not fully reconciled with T22's existing photo-10 description).

**Addendum reconciliation (v9.43):** `THREADS-v9.20-addendum.md` was written against v9.19 and cannot be merged literally: its proposed T26/T27 IDs are now occupied. Its streaming/LOD content belongs to T24 (with chunk-region metadata in T23 and stable feature identity in current T26); communications becomes T29; multiplayer/server authority expands reserved T11; spatial distortion extends current T27's zone sampler and the shared T23/T25/T5 compose seam. The addendum remains as provenance, while `plans/addendum-reconciliation-implementation-v1.md` is the executable handoff.

**Referenced by this file as an owning/handoff doc, confirmed missing from the repo (checked 2026-07-22):** `pursuer-extraction-prompt-v3.md` (T3, already flagged), `pursuer-embodiment-prompt-v1.md` (T4), `dissonance-throw-distraction-prompt.md` (T18), `wildlife-expansion-prompt-v1.md` (T19), `acoustic-world-systems-prompt-v1.md` (T20 — its own text says "this session; save under this name," which apparently never happened), `mobile-control-prompt-v1.md` (T12 — this file already says "none yet"), `region-system-prompt-v1.md` (T23 — already says "none yet"), `forest-performance-prompt-v1.md` (T24 — this file names it as if it exists), `dta-session-notes.md` (T22 §1–4, T10, T15/T16 — "the notes doc is the owning artifact," and it doesn't exist), `hero-tree-poc-prompt-v1.md` (T22 — moot now, see below: the work it would have specified shipped anyway), `rigging-track-notes.md` (T17 — already says "none yet").

**Practical effect:** every "Status: X, see owning doc Y" line for T4/T9(partial)/T12/T18/T19/T20/T23/T24 in this file is describing content that lives only in this document's own prose summary, plus whatever the original web session actually said — there is no fuller source to go back and re-read. Where this file's summary is the only surviving record, treat it as authoritative rather than provisional.

---

## Active threads

### T3 — Pursuer extraction 🔄 AUDITED 2026-07-17
Extract `PursuerSystem.ts`, `PursuerBody.ts`, `PursuerAudio.ts` into monorepo packages.
- **Handoff:** `pursuer-extraction-prompt-v3.md` (superseded v2 — **neither v2 nor v3 exist as committed files**; only the original unversioned `docs/monorepo/pursuer-extraction-prompt.md` is in the repo. v3 was pasted into a local session directly, not saved. If this thread runs again, decide whether to commit v3 as a file first — P7 says handoff prompts are the primary artifact.)
- **Phase 0 audit (local session, 2026-07-17) — actual state found, contradicting this entry's prior "ready to run" status:**
  - **Phase A (PursuerSystem → package): already fully done**, and not recently — `packages/pursuit/src/PursuerSystem.ts` is config-injected (constructor takes `PursuerConfig`, app supplies it via app-local `buildPursuerConfig()` at the call site, zero implicit config import), imports `PursuerState`/`PursuerModel` from `@dissonance/shared-types` rather than redeclaring them, `update()`/`getModel()`/`reset()` all present. Build-green gate confirmed (`turbo build --filter=dont-turn-around`, cache hit — no test runner exists in this repo, so "tests pass" is vacuous).
  - **Phase B (PursuerBody split): already done to spec.** `packages/glow/src/HeartbeatGlow.ts` holds the pure BPM-pulse math; `apps/dont-turn-around/src/pursuer/PursuerBody.ts` stays app-local for the procedural mesh build, gait animation, and illumination reaction — which matches this phase's own "render wiring stays app-side for now" scope, it isn't a gap. No code changed this session (nothing to visually regression-check).
  - **Phase C (PursuerAudio → `@dissonance/audio`): deliberately NOT done.** `PursuerAudio.ts` still carries its own `// EXTRACTION CANDIDATE` comment verbatim, which explicitly argues against extracting now: *"Revisit extraction when a second app... needs the same scheduling shape."* No second consumer exists yet. **Decided this session (Dan): honor the marker's own reasoning over the handoff prompt's literal instruction — do not extract a single-consumer "reusable" package.** This is the standing rule restated (P1 extend-don't-rewrite / CLAUDE.md "do not over-extract"), not a new one.
- **Contradiction found and surfaced, not resolved: package naming.** Every package in this repo is `@dissonance/*`; no `@dta/*` scope exists anywhere. The v3 handoff prompt (and this doc's own D-series/T-series text, e.g. `@dta/audio`, `@dta/pursuit`, `@dta/input`, `@dta/persistence` throughout) uses `@dta/*` as if it were real. Read every `@dta/*` reference in this document and any pursuer-extraction prompt as `@dissonance/*` until someone does the actual find-replace. **Not fixed here — surfacing per conflict rules, this doc is single-writer per session and a blanket rename touches too much to do as a side effect of a Phase-0 audit.**
- **Contradiction found and surfaced, not resolved: HeartbeatGlow/HeartbeatAudio BPM sync doesn't currently exist.** `HeartbeatGlow.ts`'s comment claims its pulse constants are "matched to HeartbeatAudio's BPM curve," but `packages/audio/src/HeartbeatAudio.ts`'s `setStressLevel()` is a no-op — `bpm` is hardcoded at 65 and never varies. There is no `'heartbeat-v1'`-addressable constant anywhere (the v3 prompt's Phase C asked for one). Whoever picks up stress-driven heartbeat audio next (T9, or a future pass on this thread) needs to either wire `setStressLevel` for real or correct the comment — left open.
- **Open (needs Dan sign-off, not resolved here):**
  1. Does T4 (embodiment) actually need Phase C (PursuerAudio extraction) to start, or only A/B (both already done)? T4's own text wants an `AudioProfile` composable axis, which may or may not require `PursuerAudio` to already be a package. If T4 only needs A/B, it may already be unblocked.
  2. Does T20's "Phases 2+ queue behind T3" (O8) still hold, given T3's audio-touching phase (C) isn't happening now? If T3 is never going to touch `@dissonance/audio` on its current trajectory, O8's queueing logic may need revisiting.
- **Sequencing note (unchanged):** the four-bus audio scaffold (D1) should be stood up whenever the audio phase does happen, even if three buses start empty — route pursuer audio correctly the first time rather than re-plumbing later. Not yet relevant since Phase C didn't run.

### T4 — Pursuer embodiment / profile system
Three composable axes — `BehaviorProfile`, `EmbodimentProfile`, `AudioProfile` — composing into a frozen `ResolvedPursuerProfile` at load time.
- **Handoff:** `pursuer-embodiment-prompt-v1.md`
- **Horizon variants:** drone (procedural animation) and rigged mech dog (skeletal), ground-snap raycasting, loud glTF clip-name validation at load. Acoustic-world prompt adds: damaged / hacked / abandoned / animal-imitating variants, and a **fourth axis — `SensorProfile`** (directional mics, vibration sensing, human/animal-response measurement, challenge-response) → schema change to the resolved profile (O7; embodiment prompt needs v2 before running).
- **Perceptual identity resolution:** the dog ambiguity ladder now has a data structure — `PerceptualLODProfile`, *independent of geometric LOD*: far = shared silhouette (+ audio profile), medium = behavior tell + animation divergence, near = material/anatomy truth, inspect = provenance. Identity is never resolved through labels; hacked-dog tells (holds distance, head-tracks too long, delays patrol return, listens to nothing audible) live in `BehaviorProfile`.
- **Dog silhouette ambiguity ladder (design scope):** one silhouette, five readings — (1) live dog (comic relief), (2) acoustically damaged animal (ties to creature-damage canon proposal), (3) mech pursuer (threat), (4) broken mech — dead actuator, damaged gait, ignoring player, walking an unmaintained patrol route forever (decayed-system storytelling; one extra gait clip), (5) hacked mech — surveillance asset watching the player; plugs into `applyDisruption` + break-and-hack seeds. Resolution requires closing distance. Hacked variant needs a subtle behavioral tell (holds distance; head-tracks a half-second too long) — a `BehaviorProfile` difference, same skeleton. **"Hacked by whom" stays deliberately unanswered** pending resistance open question. *Note: the ghost-of-you resolves-toward-player arc applies to the primary DTA pursuer; ladder variants are separate entities.*
- **`applyDisruption` consumer contract (T31 extension, 2026-07-31):** the captured-drone emitter is intended to become the first real caller of the documented future hook: `kind` covers sensor flicker / audio-channel jam / brief patrol-read corruption and `magnitude` is a decaying EM charge. No code symbol or caller exists yet; this is a future signature-coordination point, not a second declaration and not shipped by T31 acquisition v1. Shared pursuer behavior remains unchanged.
- **Depends on:** T3
- **Status:** designed, blocked on T3 — **but see T3's 2026-07-17 audit: Phases A/B (the parts T4 most plausibly needs) are already done; whether T4 can start now or genuinely needs Phase C (PursuerAudio extraction, deliberately deferred) first is an open question (T3's open item 1), not yet resolved either way**
- **Creature silhouette direction (new, from `creature-silhouette-direction-v1.md` in `docs/plans/` — previously unreconciled here; paired reference image `docs/plans/animals.webp`, a crystal-construct mech-animal concept sheet):** crystal material is **rejected**; the low-poly faceted-silhouette *language* is retained. Design principles proposed for freeze: silhouette carries identity, surface carries ~zero interior detail; uncanny = recognizable-animal-profile + hard-plane faceted geometry (wrongness from the mismatch, never from added monster features); environment contrast is the amplifier (creatures stay faceted, environment stays organic — do not let foliage drift toward stylized low-poly to match); **motion is the wrongness dial** — watchers read as unnatural stillness, pursuers as too-fluid for their own geometry, ambient animals get correct natural motion as the control group that makes the others legible. Poly budgets: 300–800 tris ambient, 1.5–3k hero/watcher, ceilings not targets. **Motion wrongness as data where possible:** playback-rate curves, interpolation smoothing, hold durations as `EmbodimentProfile` params before reaching for bespoke clips — same "profile, not bespoke code" pattern as the rest of T4. Runtime/authoring specifics (flat shading, no normal-transfer, export path) live under T17 below, since this is exactly the "two normal disciplines, one pipeline" split T17's tree work already established (opposite of the bent-normal foliage rule). **Open, needs sign-off:** creature surface default (matte-dark with uniform low spec, so carry light produces facet-flare proximity feedback vs. fully matte); motion-wrongness-as-profile-params vs. authored-per-creature clips (cost/expressiveness tradeoff, can hybridize).

### T18 — Throw & distraction system 🔄 UPDATED
**Pivoted from melee to strictly nonviolent.** The stick-as-stagger-weapon concept is cut (see D2). Throwables — rock, stick, bottle, pinecone — are sound-making distractors only: analytic ballistic arcs (no physics engine), one optional bounce max, impacts resolve `(profile × surface)` → noise event on the existing bus + spatialized one-shot via the existing wind/water/foliage audio pattern (Phase 0 recon gate).
- **Handoff:** `dissonance-throw-distraction-prompt.md` — **supersedes** the pickup+swing+stagger prompt entirely.
- **Future hook (documentation-only):** proposed `applyDisruption(kind, magnitude)` on the brain interface; no code symbol exists yet.
- **First intended caller:** T31's emitter, once its v2 interference verb is unblocked. That made masking window is the deliberate sibling of T18's noise distractors and T25's found thunderclap window: one signal grammar, escalating from ambient to directed. No caller is wired yet.
- **In-phase sign-offs reserved for Dan:** bottle per-surface break rules; thrown-object persistence/re-pickup (touches registry re-add path); overlapping-noise precedence (latest vs loudest).
- **Status:** handoff ready

### T19 — Wildlife / fauna
`FaunaSystem` (per-agent WANDER → ALERT → FLEE → despawn) + `AmbientSwarmSystem`. Birds audio-primary with flush events; birdsong silences at ~1.5s as earliest player warning, recovers slowly. Fireflies night-only, yellow-green only (see P5).
- **Handoff:** `wildlife-expansion-prompt-v1.md`
- **Extension path (per acoustic-world prompt):** exposure/entrainment/cumulative-dose states per agent (`AcousticSensitivityProfile`, `AcousticExposureState`); expanded state roster (freeze, investigate, repeat-route, abandon-nest, entrained, fail-to-recover); ecological conditions layer (time/season/weather/human activity/Synod exposure). **Healthy baseline first (P9)** — damaged behavior only reads against an established normal. "Specific wrongness" roster, never a generic creepy layer: birds at wrong times, alarm calls without predators, synchronized insect silence waves, animals answering infrastructure tones, moving bands of silence. Damaged animals are not monsters — confusion, timidity, repetition, detachment, wrong-time activity.
- **Status:** handoff ready; extension queued behind acoustic domain types

### T20 — Acoustic world systems — master engineering spine 🆕
Sound as a first-class simulated world phenomenon whose audible, biological, behavioral, ecological, and institutional meanings stay **separate but interconnected**. The DDI braid: fiction (Synod acoustic control) → design (command profiles, voice rig, ambiguity) → implementation (typed domain, buses, workers).
- **Owning doc:** `acoustic-world-systems-prompt-v1.md` (this session; save under this name)
- **Eight-layer separation (architecture law):** source / propagation / audible rendering / biological exposure / cumulative dose / behavioral response / ecological response / narrative interpretation. Audio playback is never authoritative gameplay state — a `SoundField` exists whether or not the player hears it. Emitters emit fields; **no `makeEveryoneKneel()`** — other systems independently process exposure and learned behavior.
- **Typed domain:** `SoundField`, `AcousticZoneProfile`, `AcousticHabitatProfile`, `AcousticSensitivityProfile`, `AcousticExposureState`, `AcousticCommandProfile` (advisory / civic-control / enforcement / experimental / obsolete), `HumanAcousticResponseContext`, typed `WorldSignal` bus.
- **Human response is never binary compliance:** recognition × conditioning × fear × physiology × social pressure × protection-of-others. A person kneels by choice, fear, imitation, or body-before-mind — **the system supports ambiguous outcomes; the person may not know which it was.** That ambiguity is the project.
- **Voice rig / vocoder (supersedes oscilloscope-as-mechanic):** mic → gate → compressor → pitch/formant → vocoder|direct → filter → distortion → delay → convolution → out, with parallel destinations (audible / authentication-analysis / recorder / in-world transmitter). **What the player hears and what the machine evaluates may differ.** Authentication is multi-dimensional (`VoiceAuthenticationResult`: identity, biological confidence, challenge timing, environmental match, anomaly score) — static playback fails advanced systems; partial matches produce graded consequences (opens door + logs anomaly; fools mech but not human; passes machine sounding monstrous). **Anti-minigame rule: never one correct frequency.**
- **Diegetic interface (P6):** no DAW rack in-world — salvaged knobs, patch cables, pipes, broken radios, rooms as processors (`EnvironmentalAudioProcessor`: mausoleum reverb, water-tank resonance, mic placement). The world is part of the processing chain. Dev HUD gets the numeric controls.
- **Mech-dog challenge slice (Dissonance vertical slice):** public command pattern → ambiguous crowd choreography → dog flags delayed compliance → challenge → Milo answers through the rig → Lola derives a carrier from the dog's own actuator hum → authentication passes → **nearby wildlife reacts badly** — technical success, ecological/social cost. Dog stays partially autonomous; player controls only part of the system; never a combat drone.
- **Phases:** 0 audit (mandatory gate) → 1 typed acoustic domain + tests → 2 Babylon/Tone bridge → 3 healthy ecology → 4 Synod fields → 5 voice rig → 6 mech-dog challenge → 7 vertical-slice integration. **Sequencing:** Phase 1 is pure types — parallel-safe now. Phases 2+ touch `@dta/audio` → queue behind T3 (single-writer rule; see O8).
- **Baseline milestone contains zero Synod effects** — forest edge, service road, parking lot, small residential/utility area; ordinary warning speaker, ordinary voices, ordinary silence. Synod overlay is a second milestone.
- **Status:** owning doc ready; Phase 0 audit is the next local session after (or parallel-typed alongside) T3

### T17 — Character rigging learning track
- **Status:** active — offline/Blender track, parallel-safe, zero repo conflict
- **Scope:** four-tier skill ladder building toward production character assets. Each tier proves a pipeline capability before the next adds complexity:
  1. **Segmented rigid arm** — three-cube chain, plain bone parenting (no weight painting), single keyframed action, full Blender → `.glb` → Babylon `AnimationGroup` round trip. *Walked through in full; ready to attempt.*
  2. **Mech quadruped** — rigid-segment technique at production scale; walk cycle; feeds T4 mech-dog embodiment directly
  3. **Organic quadruped** — first real weight painting (Automatic Weights + cleanup); feeds T8 live dog / animals
  4. **Humanoid + outfit** — full deformation rig; target: 3D Lola character. Nightmare-Lola = same rig, corrupted animation / shader treatment, not a second model
- **Known failure modes (tier 1):** wrong import scale (fix: Ctrl+A Apply All Transforms pre-export), animation plays but nothing moves (keyframes set in Object Mode instead of Pose Mode)
- **Conventions:** bone and Action names are load-bearing — they travel into the glb and become Babylon lookup keys; validate clip names loudly at load (same rule as T4)
- **Creature authoring discipline (from `creature-silhouette-direction-v1.md`, see T4 for the design principles):** flat shading only, split normals per face, **no** Data Transfer normal baking on creatures — the deliberate opposite of the foliage pipeline's bent-normal rule (T22's hero/scatter tree tier). Model to side/three-quarter profile first, validate as a black silhouette against mid-grey before any material work. No textures — flat material or vertex color, UV unwrap optional. One exaggerated identifying shape per creature (ears, tail, posture, head-drop), named in the asset's README line. Export path unchanged: Blender → glTF `.glb`, `AnimationGroup` clip naming per the loud-validation rule. **Note the two normal disciplines don't cross-contaminate** — this is the one place in the pipeline where a habit from one asset class would visibly break the other.
- **Ties:** T8 (this track produces its skills), T4 (mech dog + stagger clip + creature silhouette direction), T-Lola (tier 4 target; one-rig-two-presentations rule)
- **Numbering note (RESOLVED v9.3):** T4 = embodiment thread above; T8 retired — merged into this thread's asset queue. See registry.
- **Asset queue (carried from v8, extended v9.7):** uprooted **root ball** (figure-silhouette deadfall — landmark/false-scare/interactable prop; loose pushable limbs rigged as separate bones, motion player-caused only, per reference photo + Dan’s field note); creek-bed rock scatter (the 4–5 rock variants’ reference is the 2026-07-15 creek photo); mossy log; modular stone-step unit (random stairway kit — one step mesh, thin-instanced); picnic kit — stone table+bench set, post grill, trash barrel (metal resonators for T18), pavilion shell, dead fountain (landmark prop); giant snag (static standing deadfall, figure-adjacent silhouette); shed shell (fieldstone one-room, board gable, vented roof); live dog — false-scare double, **silhouette must rhyme with the mech dog at range while close reads diverge; design the pair together, not sequentially**; player character rig — visible in isometric surveillance view, clothing/cosmetic layer; vultures (roost pose, feeding pose, distant-circling billboard); pursuer stagger/telegraph clip.
- **Owning doc:** none yet — tier walkthroughs live in web sessions until a `rigging-track-notes.md` is worth extracting; player rig (isometric views)

### T1/T2 — Environment profiles / Dev HUD
`EnvironmentProfile` data type; `applyProfile()` as the sole engine code path. Dev HUD with live parameter tuning + export-to-JSON authoring loop.
- **HUD feature candidate (from Godot PoC):** agent route/waypoint visualization as a toggleable overlay — generalize the PoC's patrol-route toggle for any agent (drone patrols, DTA pursuer).
- **Status:** established pattern; extend, don't fork

### T21 — Geo pipeline / TrailViewer — successor foundation (per P8 correction)
- **Lineglass geospatial-reference layer (planning, 2026-07-24):** `plans/dissonance-lineglass-engineering-review-prompt.md` makes the prospective Dissonance Boulevard inspection device capable of a restrained, world-registered geographic reference: independently selectable WGS84 grid/local origin, authored civic or narrative landmarks, GPX route/waypoint traces, and, only when bundled with valid attribution, offline OSM-derived roads, footprints, and paths. The already-shipped graticule is the real substrate, not a claim that a Lineglass runtime exists. Guardrails: sources remain fallible municipal evidence (stale, redacted, offset, or ideologically classified); render selectively through the device rather than as an always-on minimap; require no live network; and degrade to the grid plus authored landmarks when richer data is absent. First Boulevard-slice acceptance: correct geo/local registration, one landmark, and an independently toggleable authored or GPX route trace. **Diegetic tiered-unlock slice shipped 2026-07-28** (see below) — still no render-layer device (structural/signal/acoustic/historical/biological contours, custom shaders, `@dissonance/lineglass`+`@dissonance/diagnostics` package split), just the §7.6 geo-reference layer's unlock gating, reusing the grid/OSM/GPX toggles that already existed.

South Mountain Reservation heightmap validated the premise: one real heightmap generates a wide variety of landscape types. `packages/geo` (WGS84→UTM, GeoJSON/GPX parsing, heightmap sampling) + `HeightmapTerrain` in `packages/world`. MapMyRun GPX as ground-truth validation.
- **Traversal/atmosphere layer (local session, 2026-07-14/15):** trail-viewer picked up **Drive mode** (`DriveController` — grounded, flight-speed traversal alongside Walk/Fly), **`WaterPlane`** (reflective, tunable real-elevation level, + separate murky underside so submerging shows no void), **`Sun`/`StarField`** day–night cycle (`setTimeOfDay(hour)` drives ambient, sky color, star visibility together), tunable **fog** (density + color picker), an **overcast toggle** (denser/greyer/lower cloud blanket + ~40% sun/ambient dim — genuinely flat diffused sky), **`ThinInstanceTrees`** (elevation-scattered, avoids the flat coastal zone — no land-use data in this dataset; tree-count slider capped at a deliberate 8000 hardware ceiling — thin instancing keeps render cost flat, so the cap is considered, not accidental), a **`ForestFire`** spread mechanic (F ignites nearest tree; neighbor spread, throttled growth, survives rescales), and a **Bounded World toggle** (clamps Walk/Fly/Drive to the DEM bbox per frame; app-local — Fly/Drive previously had no bounds concept). Full tuning state persists per level across reloads. **All are decoupled siblings of DTA’s `CloudSystem`/`DaylightSystem`/`ForestGenerator`/`PlayerController`** — those are coupled to `ExperienceProfile` and DTA’s ~800-unit scale; trail-viewer is real-world/km scale. App-local for now; no shared package until a second consumer exists.
- **Copy/Load View snapshot mechanism (net-new, never previously committed):** “📋 Copy View” serializes the exact current vista to clipboard JSON; “Load View” jumps to one. **`ViewSnapshot` shape:** `{ level, activeMode?, x/y/z?, rotationX/Y? }` (player modes) or `{ level, orbitTargetX/Y/Z?, orbitAlpha/Beta/Radius? }` (orbit), both sharing `hScale, vExag, waterLevel, cameraHeightOffset?, timeOfDay, fogDensity, fogColor?, overcast?, starCount, cloudCount, treeCount`. Reuses trail-viewer’s per-level settings storage, no parallel format. **Codebase-wide gotcha (caught + fixed):** `location.reload()`/`location.href` fire `beforeunload`/`pagehide` on the dying page *before* the new one loads — the periodic autosave was re-persisting stale in-memory state and clobbering the just-written snapshot (same race fixed once before for reset-position). Fix: unregister those listeners immediately before writing + navigating. **Remember for any future reload-to-restore-state pattern.**
- **Archive format formalized (Dan, 2026-07-17):** the hand-pasted view collection is now `apps/trail-viewer/docs/views.json` — a proper JSON array of `{ name, ...ViewSnapshot }` objects, replacing the old `views.md` (name-line + fenced-looking-JSON-block markdown, informally accumulated, never actually valid JSON as a whole file). Migrated all 3 existing entries verbatim (values unchanged) via a parse script, not hand-transcription, to avoid digit errors in the position/rotation floats. Copy View still writes a single bare object to the clipboard; a human still pastes it in as a new array entry by hand.
- **Saved-views dropdown (same day, follow-up):** `ViewToolsRow` renders a "Saved views" `<select>` above Copy/Load View, in both the orbit and player mount sites, sourced from `docs/views.json`'s array. Picking an entry reuses Load View's exact apply path (validate `level`, unregister autosave, `saveSettings`, reload-or-navigate) — extracted into a shared `applySnapshot` so the pasted-textarea and dropdown paths can't drift. Dan added a 4th entry, `dissonance-forest-scale` (level 1, `hScale: 1, vExag: 10` — his candidate scale for porting the DTA/Dissonance concept onto this terrain, ties to T21's DTA-onto-TrailViewer generative-insight note above). Verified via Playwright in both modes; picking `dissonance-forest-scale` correctly teleports to its exact saved position. **Still a one-way, manual archive** — Copy View doesn't write into `views.json` itself, a human still pastes a new entry in by hand; auto-appending is the natural next step if this becomes routine, not built now. **Correction (2026-07-17, later same day):** the file was moved from `docs/views.json` (static import) to `apps/trail-viewer/public/data/views.json` (runtime `fetch`, matching heightmap/trails/gpx) — the "no relocation needed" note above no longer holds. Confirmed live: a 5th entry (`spooky night`) Dan added mid-session showed up with zero rebuild, which a static import never would have.
- **HUD redesign (2026-07-17):** the control panel was a flat, ungrouped stack (6 checkboxes each on their own line, no section separation) — reorganized into labeled `Section`s: **Toggles** (all checkboxes in one 2-column grid — visibility ×6 + Overcast + Windy + Bounded-world, was the main "wasted space" complaint), **World** (tree count + H-scale/V-exagg/water-level — tree count moved out of atmosphere since it's density, not sky), **Sky** (time/fog/stars/cloud-density only), **Movement**, **Navigation & Views**, **Audio** (new, see below). Panel made scrollable (`max-height: calc(100vh - 56px); overflow-y: auto` — pure CSS, no resize listener needed, vh recalculates natively). Added a "📍 Copy current" lat/lon control (`worldToLatLon`, new mirror of `latLonToWorld`, added to `packages/geo`) for hand-building the landmark manifest. Added a full-screen loading overlay covering first load and the app's several reload()/navigate flows (Load View, reset-position, saved-views dropdown), hidden once the scene is actually ready to render.
- **DTA weather system ported (2026-07-17):** `WeatherSystem` (`@dissonance/world`) reused directly, no decoupled sibling needed — audited and found already fully generic (no `ExperienceProfile` coupling, the `Scene` constructor param is unused). Added a "Windy" toggle (Toggles section) wired to `WeatherSystem.setMode()`; cloud drift speed reads the toggle directly (3x when windy) rather than the live ramping `windIntensity`, since clouds only rebuild occasionally and the intensity ramp (~2.5s) would rarely be reflected — `weatherSystem.update()` still runs every frame to keep gust dynamics alive for future consumers. `weatherMode` persisted through `SavedSettings`/autosave/both Copy View snapshots per Dan's "persist all environment values" instruction.
- **DTA player/audio systems ported (2026-07-17):** audited `packages/player`/`packages/audio` before porting anything (see verdicts below) rather than assuming a straight copy. **Finding that changed the shape of the work:** `BreathSystem` wasn't something to port — `PlayerController` already constructs and runs it every frame in trail-viewer, unread by anything; this pass just surfaces it (live breath-load readout, new **Audio** `Section`). `AdrenalineSystem.update()` hard-requires DTA's `PursuerState` (no pursuer in trail-viewer) — **not ported**, flagged as a follow-up idea (`forestFire` proximity is a plausible substitute driver) rather than built speculatively. Ported as-is (already fully generic, zero DTA coupling): `AmbientAudio` (wind bed + night drone, wired to the existing `dayFactor`/`WeatherSystem` outputs — both already computed with no audio consumer until now) and `HeartbeatAudio`. New decoupled sibling `TrailPlayerAudio` (`packages/audio`, mirrors DTA's `PlayerAudio` breath-handling exactly, but calls a new `AudioEngine.playTrailStep()` — open dirt/gravel character — instead of `playForestStep()`) plus an independent footstep-mute toggle DTA's version doesn't have. **Not ported:** `RiverAudio` (no "distance to river" signal — `WaterPlane` is a flat elevation plane, not a tagged river feature), `DestinationAudio` (DTA-specific car-alarm content for a "destination" concept trail-viewer doesn't have), `AudioEngine`'s pursuer/forest-specific methods (no pursuer). **AudioContext unlock:** trail-viewer had no user-gesture point at all (unlike DTA's menu/Begin-button) — added a persistent "🔊 Enable Audio" button next to the HUD toggle, confirmed with Dan over the alternative (a one-time full-screen prompt). Audio settings (`masterMuted`, `windVolume`, `footstepMuted`, `breathMuted`) persisted the same way as every other field this session. `audioEnabled` itself is **not** persisted — browsers require a fresh gesture on every load regardless of what was previously true, so there's nothing a persisted flag could actually restore.
- **Mode-select bug fixed (2026-07-21):** Dan reported "the travel select is still broken" — a prior local session had already staged a fix (blur the `<select>` on change, guarding against native type-ahead stealing W/D keystrokes) in the uncommitted working tree, but that wasn't the actual cause. The real bug, pre-existing and untouched by that fix: `MovementRow`'s onChange wrote `signals.activeMode.value = mode` directly *before* calling `onModeChange` (→ `switchMode` in `main.tsx`), and since `signals` is the same object as `switchMode`'s own `movement`, `switchMode`'s dedup guard (`if (newMode === movement.activeMode.value) return`) saw no change and bailed before running any of the real handoff — position transfer, ground-snap, rotation copy, and `scene.activeCamera` reassignment all skipped. Symptom matched exactly: dropdown label and readout relabeled correctly (both read the same signal), but the camera never switched and WASD drove an invisible controller. Fix: `MovementRow.tsx` no longer sets the signal itself — `switchMode` already does that as the last step of the full swap. Both fixes (blur + this one) now coexist in the working tree, uncommitted.
- **Lat/long graticule overlay shipped (2026-07-22, confirmed in code — resolves a NOTES.md parking-lot item):** `geo-grid-engineering-prompt.md` (found committed in `docs/plans/`, previously unreconciled here) specified a toggleable WGS84 lat/lon grid over the terrain, explicitly to support hand-placing entries for the landmark manifest — this is `docs/NOTES.md`'s untriaged item 2. It's built: `packages/geo/src/graticule.ts` (`graticuleLines()` — snaps to the smallest set of interval multiples covering the bbox, samples each line through the real UTM→lat/lon projection rather than drawing a straight chord, so a visible kink would flag a projection bug) wired into `apps/trail-viewer/src/main.tsx` as a toggle layer. NOTES.md's item 1 (Mac vertical-elevation bug) remains untouched/uninvestigated.
- **Stuck-movement-key fix (2026-07-22, committed, no prior THREADS entry):** a key held down when window focus leaves (alt-tab, click into another app) never receives its matching keyup — the browser only delivers that to whichever window has focus at release. Left alone, the stuck key reads as permanently held, and a second real keypress after refocusing can cancel out against it (stuck W + real S = dead movement). Fixed identically across all three movement controllers (`PlayerController`, `FlightController`, `DriveController`): clear all key state on `window blur` and `visibilitychange`(hidden).
- **WaterPlane no longer requires a remote texture (2026-07-22, committed):** `WaterPlane` gained an optional `bumpTextureUrl` — `undefined` keeps the historical Babylon-hosted sample bump texture (no behavior change for existing callers), `null` skips the bump texture entirely for callers that want to stay fully local/offline.
- **@babylonjs/loaders dependency added (2026-07-22, committed):** prerequisite for the glTF hero-tree loading work above (`TreeAssetPreview.ts`, `HeroTreeInstances.ts`); deduped/excluded in `vite.config.ts` the same way `@babylonjs/core` already is, so pnpm's per-workspace symlinks don't make Vite discover and re-optimize it from multiple paths.
- **Slope-blended 3-tier ground material landed, 2026-07-23 (superseded a same-week single-texture pass — see below):** `HeightmapTerrain` (`packages/world`) gained a `slopeTextures: { flat, mid, steep }` option — real tiled ground textures blended by DEM-derived slope (finite-difference on `HeightmapSampler`, 8m offset; no external land-cover data exists for this dataset — same gap `ThinInstanceTrees` already notes) in place of the flat low/high-elevation vertex-color gradient. Layers: `forest_ground_04` (flat), `rocky_terrain_02` (mid), `marble_cliff_03` (steep) — all Poly Haven CC0. Blend weights are baked once into a procedurally generated 256x256 `DynamicTexture` (R/G/B = flat/mid/steep weight) feeding Babylon's `TerrainMaterial` (`@babylonjs/materials`, already a dependency via `WaterPlane`'s `WaterMaterial`, so no new install). **Real gotcha, caught not anticipated:** ffmpeg's EXR decoder doesn't implement single-channel "Gray DWA" compression — silently decoded affected roughness/normal maps to all-zero pixels with no error at all, caught only by sanity-checking raw pixel values before trusting the output; Blender 5.2 (found installed but not on PATH) used instead via `--background --python` for every EXR-sourced map, colorspace-verified the same way. **Mesh UV mapping was verified against Babylon's `CreateGroundVertexData` source, not assumed** — needed to know whether U/V tracked world X/Z directly or with an axis flip, to generate the mix texture at the right orientation; got it right on the first attempt, confirmed visually (steep faces show the rock-toned layer, gentle slopes show the flat one). **Known tradeoff:** `TerrainMaterial` is Babylon's legacy Phong-lit splat material, not PBR — one global `specularColor`/`specularPower`, no per-layer roughness/metalness/displacement at all; chosen over hand-writing a custom PBR splat shader. **Superseded pass (landed same week, never separately committed):** an earlier single-texture version (`rocky_terrain_02` alone via `PBRMaterial`, with a roughness-from-metallic-texture-channel trick) shipped first and is fully replaced by the above, not layered underneath it — its `spec`/`disp`-map-unused gaps no longer apply (this material has no roughness or displacement slots for *any* layer, so nothing was silently dropped, there's just nothing to fill). **One gap that still stands:** the HUD's "Terrain (low/high)" color-picker swatches remain dead controls — still wired through, but do nothing once `slopeTextures` is set, and no toggle exists between the two modes.
- **DTA environment port scoping doc added, 2026-07-24 (`dta-environment-port-prompt-v1.md`, `docs/plans/`):** Dan asked to replace "the first DTA level environment" with the `dissonance environment test 1` view's data (this thread's own `dissonance-forest-scale`/v9.18 exploration, next iteration — now covering atmosphere + all three forest tiers, not just H/V-scale). Found it isn't a config edit: DTA's `Terrain`/`ForestGenerator` (fixed 800-unit procedural world, `ExperienceProfile`/`RunProfile`) share no fields with trail-viewer's DEM/scale-tuning/tiered-forest systems for roughly two-thirds of that view's params (hScale/vExag/waterLevel, sky/water/terrain colors, treeRegionRadius, bulk/trailside/hero tier params all have no DTA-side home today). Audit-only doc, five open questions flagged for Dan's sign-off before any implementation session, explicitly framed as feeding T23's existing "DTA-onto-TrailViewer work items" list rather than competing with it — see T23 below and the doc itself for the full field-by-field mapping.
- **Fast-travel design intent (noted, not built):** Fly/Drive read naturally as *unlockable* fast-travel skills (gated behind landmark/item), not defaults — needs `@dissonance/persistence` (still a stub) and an actual progression reason.
- **Generative insight (cross-ref T6, T23):** same DEM + H-scale/V-exaggeration levers read as seeds for **distinct games**, not one viewer — skiing (Drive + downhill-biased physics, not built), firefighting (`ForestFire` is most of the way there), climbing/bouldering (steep-terrain-aware Walk variant, not built). Extends T21’s “one heightmap → many landscape types” premise from visual variety to game *mechanics*. Insight, not scoped work.
- **Foliage wind sway — scoped down from `dissonance-camera-aware-foliage-prompt.md` (session, 2026-07-23):** Dan brought a large from-scratch engineering prompt (near-camera dissolve, a third-person camera-to-player visibility corridor, player-push displacement, optional Synod modulation hooks) for review. Repo-reality check found most of it doesn't apply here yet: DTA's `ForestGenerator` foliage is solid-color procedural geometry (cones/spheres/planes) with no alpha-tested leaf texture to dissolve; there's no third-person camera or `apps/dissonance-*` workspace for the corridor/Synod pieces to attach to. Corridor/dissolve/push/Synod are shelved, not built. Scoped down to the one slice that's real and wanted now — **wind sway** — for the trees Dan is actively building: `bulkForestScatter.ts`'s decimated-`ThinInstanceTrees`-candidate-pool tier, plus the trail-adjacent hero-zone `HeroTreeInstances` GLBs (both trail-viewer, both share the same `loadHeroTreeInstances` loader across the bulk-forest/trailside/hero-zone clusters).
- **Contradiction found, missed by the v9.22 doc-inventory pass:** `FoliageSwayPlugin` is cited as already existing in three places — T22's engineering bullet, T22's hero/scatter-tree tier bullet, T24's Phase-0-audit origin bullet (the latter also asserts three-zone LOD already exists) — since v9.7/v9.20. A repo-wide search turns up zero code under that name, or any vertex-shader wind implementation at all; `generation-systems-audit.md` (2026-07-07) independently confirms both "GPU wind sway via `MaterialPluginBase`" and "3-zone distance LOD" as **Absent**. Same phantom-doc pattern the Doc Inventory section already tracks, just never flagged for this specific name — corrected inline at both mentions, not rewritten.
- **What's actually reusable:** `WeatherSystem` (`@dissonance/world`) already runs a real wind-gust simulation (`windIntensity` 0–1, ramped + gust timer) and is already ported to trail-viewer (this thread, v9.19) — that entry already notes `weatherSystem.update()` keeps running every frame "to keep gust dynamics alive for future consumers." This is that consumer.
- **Implemented (2026-07-23):** `FoliageSwayPlugin` is now a shared `@dissonance/world` `MaterialPluginBase` vertex displacement driven by `WeatherSystem`'s live `windIntensity` plus its new shared time accumulator. Thin-instance world-matrix translation is hashed in the vertex shader for stable per-tree phase variance (no new instance attribute buffer). It is attached only to `ThinInstanceTrees` canopy materials and the `leaves`/`twig`/`twigs` leaf-card materials discovered in `HeroTreeInstances`; trunks, bark, branches, dead wood, and stumps stay rigid. Every trail-viewer tree tier receives the same already-running `WeatherSystem` instance. DTA's `ForestGenerator` uses the same trunk/canopy-separate-material convention and could adopt this later with no new plumbing — not scoped today.
- **Boulevard visual tuning + utility corridor (2026-07-27):** the city-kit buildings (`CompositeLocations.ts`) gained a `windowTintColor`/`windowGlow` HUD-tunable pair (default white/0 — every existing view keeps today's un-tuned look unchanged) plus a conservative bloom-only `DefaultRenderingPipeline` pass so the tint actually reads as a glowing window. The kit's baked "fake interior" photo card (`MI_FakeInterior_*` — a photograph of a little room, tiled identically across every window at scale, reading as a repeated/mundane office photo rather than an atmospheric window) was replaced with a plain procedural glow-gradient card carrying the same tint/glow, plus one still silhouette (T7's "window occupancy" idea — a small static cut, not the full rarity/motion spec) composited onto one building type's card. A new `"dissonance boulevard concept art"` `views.json` entry captures the tuned violet-window night look this was built against. Separately, `LocationEntry` gained a `corridor` field (freeform polyline, `LocationProps.ts`) and a new sibling module `world/UtilityCorridors.ts`: thin-instanced utility poles (`buildUtilityPole`) walking a corridor's polyline at a fixed spacing, plus one `CreateLineSystem` mesh per corridor carrying sagging multi-wire spans (parabolic sag; wires vertically staggered rather than laterally offset, sidestepping a crossarm-orientation correctness risk). This is the first real content built against the T7-substrate resolution below (runtime/data-hybrid placement, no offline manifest) and the first shipped slice of the rural-infrastructure feature request (see new T28) — one utility corridor now crosses "dissonance boulevard" near its crosswalk (`locations.json`). A "Power lines" HUD toggle was added gated to non-orbit mode only — caught before shipping: wiring it into the always-mounted `VisibilityToggles` component would have thrown in orbit/level-3 mode, since `utilityCorridors`, like `compositeLocations`, only loads in the non-orbit path.
- **Building collision + Milo's building interior (2026-07-27):** `CompositeLocations.ts` now returns a `Collider` per building placement (radius from each glTF's real footprint bounds — checked via each mesh's position-accessor min/max, not guessed: small 12.46x14.54m→6.5, medium 15.06x13.06m→7, large 20.64x16.64m→9), fed into `PlayerController` alongside props/poles via a new `applyPlayerColliders()` in `main.tsx`. `LocationEntry` gained an optional per-placement `id`; one building-medium on "dissonance boulevard" is tagged `"milos-building"` in `locations.json` and excluded from the collider list, so it alone stays walk-through. **Milo's building then got real interior geometry** (Dan: "add a door? and an interior stairwell that leads to a second floor apartment?") — `Building_Medium_2_001.gltf` has no real door gap or second story of its own (its front is one continuous `MI_Glass` pane, checked directly; `MI_InteriorWall`/`MI_InteriorFloor` are a flat one-sided ground-floor backdrop, not an enclosed room), and thin-instancing means unique geometry can't be added to the shared "building-medium" template without appearing on all six instances on the boulevard. Milo's placement is now excluded from that shared batch and loaded as its own standalone container (`loadMilosBuilding`, `placeSingleTransformMesh` — a regular Node transform instead of a thin-instance matrix, specifically so the extra geometry can be parented to it), carrying a new `buildMilosInterior` (`LocationProps.ts`): a visually distinct door, an 11-step stairwell (~4m rise), and an open-topped second-floor room shell, all positioned from the building's own scanned local bounds. **This exposed a real gap, fixed the same session:** `PlayerController` positioned the player purely from outdoor terrain height, no concept of standing on indoor geometry at all — climbing the stairs wouldn't have raised the player. `@dissonance/player` gained `FloorSurface` (`{minX,maxX,minZ,maxZ,y}`) + `setFloorSurfaces()`: each frame, if the player's x/z falls inside a registered rectangle whose `y` is above current ground but within a small step-up tolerance (0.6m — clears each stair's ~0.36m rise, rejects snapping straight up onto the ~4m second floor from ground level), that height wins over the outdoor terrain sample. `CompositeLocations.ts` converts `buildMilosInterior`'s local-space rectangles to world space via the same rotate/scale/translate math the building itself is placed with (exact for the 90-degree-multiple rotations every compound placement in this app uses). **Also fixed the same session, unrelated:** a stale-generation race in the async `compositeLocations` rebuild (a second hScale/vExag drag before the first finished could leave two live copies of the boulevard) — `compositeLocationsGeneration` token added, disposes a stale completion instead of applying it.
- **Shadow-map banding investigated (2026-07-27, Dan reported "weird red bacon effect" from a screenshot of the boulevard sidewalk):** diagnosed as texel-density aliasing, not a texture bug — `Sun.ts`'s `ShadowGenerator` frustum auto-fits to the whole visible scene by default, and at this world's real scale (multi-km terrain, doubled boulevard, mountain ring, thousands of trees) a flat receiver like the sidewalk exposes that as banding at low sun angles. Conservative first fix applied: map resolution 1024→2048, `normalBias` 0→0.02 — doesn't touch frustum-centering behavior. **Root cause not fully addressed**: a fixed, camera-centered `shadowFrustumSize` (shadows crisp near the player, fading at distance) is the real fix, deliberately not attempted blind — real "shadows vanish/pop at distance" tradeoffs worth seeing live first. Unverified live either way (chrome-devtools MCP connection down all session) — needs a look next session.
- **Boulevard obstacle collision + second doubling (2026-07-27, Dan: "add logic to detect all blvd player collisions... only have them for building currently" + "make blvd twice as big"):** `BUILDING_COLLISION_RADII` generalized to `OBSTACLE_COLLISION_RADII`, now also covering `bollard` (0.15, from the glTF's own 0.216x0.227m footprint), `planter` (1.0, from 2x2m), and the compound-placed `street-lamp` (0.3, reusing the same radius `LocationProps.ts`'s `PROP_COLLISION_RADII` already settled on for the identical `buildStreetLamp` mesh placed via the other path). Streets/sidewalks/crosswalk decals/manhole covers/drains deliberately excluded — flat ground, walking on them is the point. The boulevard's entire current compound (both existing blocks, 52 placements) was duplicated again at a +29-grid-unit offset (same "continue the sidewalk's own arithmetic sequence with zero gap" method as the first doubling, v9.40) — 52→104 placements, 18→36 buildings. **Learned the hard way:** the first attempt regenerated the whole file via `JSON.stringify(data, null, 2)`, which reformatted every array in the file onto multiple lines (no way to tell a generic serializer "only this one section changed, preserve the rest byte-for-byte") — reverted, redone as a text-level insertion generating single-line placement objects matching the file's existing hand-authored style, keeping the diff to the ~58 lines it should be. Worth remembering for any future locations.json edit: never round-trip through `JSON.stringify` on the whole file.
- **Underwater look (2026-07-27, Dan: "create an underwater look and apply it"):** `WaterPlane`'s existing "murky underside" mesh already gave a plausible ceiling looking up at the surface from below (Fly/Drive have no collision, so submerging is easy to do by accident), but nothing changed how the rest of the view read once actually submerged — scene fog stayed at whatever the outdoor atmosphere sliders had it set to. `main.tsx`'s player-mode game loop now compares the active controller's Y against the water plane's current render-space level (`waterLevel * vExag`, matching `WaterPlane.setScale`'s own math rather than adding a getter for one call site) and overrides `scene.fogColor`/`fogDensity` to a denser, murkier reading (`UNDERWATER_FOG_COLOR`/`UNDERWATER_FOG_DENSITY`) while below it, restoring the atmosphere-driven values the instant it isn't. Same `fogMode` (EXP2, already set once). Edge-triggered (`wasSubmerged`), not per-frame, so it doesn't fight the atmosphere sliders' own `effect()`s while surfaced. Deliberately fog-only for a first pass — a real screen-tint/caustics post-process would read better but is a separate, riskier visual change; orbit mode (level 3) untouched, same "buildings/colliders only exist in the non-orbit path" scoping as everything else this session.
- **Lineglass parts — diegetic tiered unlock (2026-07-28, Dan: "use the lineglass prompt to add a diegetic object that when parts are found allows player to see latLong/osm/gpx, but only after collecting commensurate parts"):** the full `dissonance-lineglass-engineering-review-prompt.md` is a 21-section engineering brief (6 render layers, custom shaders, a 6-package split, anomaly system, telemetry, accessibility) — this implements one small, explicitly scoped slice of it: §7.6's geospatial-reference layer, gated by §6's "progressively unlocking new scan layers" idea. Confirmed with Dan before building: progressive per-layer tiers (not one shared-pool unlock). New `state/lineglass.ts` (`LINEGLASS_TIERS`: 1 part → grid, 2 → GPX, 3 → OSM; `unlockedLineglassLayers()` pure function of collected count) and `world/LineglassParts.ts` (a small faceted cyan/amber hovering, spinning prop per part — deliberately not thin-instanced like everything else in that file's neighborhood, since each of the 3 parts needs independently controllable visibility; proximity pickup, no interact key, matching this app's existing "walking near it is enough" convention). `LocationEntry` gained a `lineglassParts` field (exact local-meter positions, same convention as `corridor.path`) — 3 parts placed on "dissonance boulevard": near Milo's building, at the crosswalk, and in the far block. Collected part ids persist through the existing `SavedSettings`/autosave path (`lineglassPartIds`), restore already-hidden on reload, and never re-award. `VisibilityToggles`' OSM/GPX/Grid checkboxes — previously always-on, ungated — are now hidden entirely (not just disabled) until their tier's threshold is reached, reading the collected-ids **Signal itself** (not a boolean snapshot) so the row appears immediately on pickup without needing the enclosing HUD section to re-render. Picking up a part also auto-enables its newly-unlocked layer's visibility, so the unlock moment is immediately visible, not just newly toggleable. Not implemented (deliberately, per the doc's own "prefer a small coherent first implementation" principle): the device object itself, any render layer beyond geo-reference, OSM/GPX gating beyond visibility (the underlying trail/OSM data was already loaded regardless), and any of the 5 other packages the full review proposes. Unverified live (still no browser connection this session) — worth confirming part positions actually sit on walkable ground and the pickup radius feels right.
- **Persistence/export parity audit (2026-07-28, Dan: "I want every editable param to be saveable to storage and to exported jsons"):** prompted by confirming `lineglassPartIds` was saved to `localStorage` but not to Copy View — found both `buildSnapshot()` functions (orbit and player mode) were missing three fields `persistSettings()` already had (`hudVisible`, `worldBounded`, `lineglassPartIds`); added to both. Broader audit turned up a real gap: every `Toggles`-section checkbox (`state/visibility.ts` — terrain/OSM/GPX/water/clouds/grid/mountains/power lines/mech dog) was explicitly session-only by original design (comment: "Not persisted anywhere ... session-only"), reset on every reload. `createVisibilitySignals()` now takes an optional defaults object; `SavedSettings` gained 9 matching `*Visible` fields, written by `persistSettings()` and both `buildSnapshot()`s. **One nuance handled carefully:** grid/OSM/GPX are also gated by Lineglass unlock state (previous bullet) — restoring naively would have let a saved `true` show a layer before its part was ever found. Fixed by keeping the two concerns separate: `unlockedLineglassLayers(...)` still decides whether a layer *can* show at all, ANDed with `savedSettings.<layer>Visible ?? true` (defaults to visible once unlocked, matching the existing auto-enable-on-pickup behavior) for whether it's *currently checked*. Since unlock is monotonic and both persist together, the two can't actually disagree in practice, but the code no longer conflates them. Includes `mechDog` — the toggle Dan's own in-progress mech-dog pursuer feature added mid-session — brought into the same persistence path without touching the pursuit logic itself. Verified with a clean `tsc --noEmit` and `vite build` (`apps/world`); not checked live (no browser connection this session).
- **Captured-drone Lineglass view (T31 extension, design-only):** a future drone-mounted render mode reuses the shipped graticule and eventual Lineglass substrate to show machine-legible geometry plus acoustic emissions, not a superior conventional camera. Textures drop away; outlines and world-space signal traces carry the read; T31 wear may corrupt those layers diegetically. This remains gated on the terminal/receiver/control loop and does **not** imply that the broader Lineglass render device has shipped.
- **Status:** POC validated and growing into the successor foundation; open decisions O1–O3 gate next steps
- **Feeds:** single-level hub structure (T16), region system (T23)
- **Downstream (added here 2026-07-15 — was previously only discoverable by reading the other threads):** T24 (forest performance & chunking — trail-viewer is its target app, shares this codebase, default-serial with general T21 dev); T25 (atmosphere grading — layers dawn/time-of-day post-processing onto this thread's existing `setTimeOfDay(hour)` cycle, doesn't replace it); landmark manifest (`landmarks.geojson` in `packages/geo`, proposed under T22 — photographed SMR features become placeable anchors through this thread's WGS84→UTM path).

### T22 — DTA forest visual & play-scenario direction
Compact index of `dta-session-notes.md` §1–4 (all PROVISIONAL; the notes doc is the owning artifact):
- **Palette:** hue-narrow / value-wide — blue-black green → acid chartreuse; crush shadows cool, let backlit foliage clip via bloom, starve red channel. Warm orange/red reserved for carry light + interactables (extends P5).
- **Play scenarios:** culvert as threshold object (water masks both hearing channels — free speed, blind approach); mow line as instant exposure gradient (tall grass conceals, costs foliage noise). Shadow-as-exposure PARKED (analytic method sketched, no shadow-map readback).
- **Creek corridor (provisional, from reference photo 2026-07-15):** the culvert scenario generalized to a line. A carved channel below grade — mostly stones with shallow water threading between them, **not** a `WaterPlane` job: rock-scatter along a spline + local water patches/channel-floor shader. Running water raises the floor on the pursuer’s hearing channel along the whole corridor → a fast, masked route that equally masks its approach — risk/reward as geography. **Stepping-stone clusters** = crossing points: slow, exposed, noisy on wet rock. **Eroded cut bank** (overhang + exposed roots) = the gentlest first terrain-stamp case (→ O3), easier than the culvert. Streambed rocks = T17’s 4–5 rock variants’ reference sheet. Dense bank undergrowth = the mow-line gradient in natural form: concealment to the water’s edge, total exposure in the channel.
- **Random stairway (provisional, from reference photo 2026-07-15):** cut-stone trail steps climbing a slope — built infrastructure deep in wild forest, unexplained. Three jobs in one feature: (1) **traversal** — the fast, legible vertical route across a steep grade; (2) **acoustic exposure** — stone underfoot changes footstep timbre from leaf-litter soft to hard, loud, and *rhythmic*; regular cadence on stone is the most classifiable gait signal the player can emit (feeds the mutual-legibility grammar); (3) **sightline exposure** — a staircase is a long axial corridor; on the stairs you are visible from both ends. Net: the stairway is the vertical twin of the trail bargain — speed and orientation bought with sound and visibility; the slow, quiet alternative is scrambling the flanking slope through undergrowth. **Tone rule:** never explained, nothing at the top is owed — stairs-in-the-woods carries its own folklore unease and the diegetic-discovery filter governs (deep-lore tie to pre-Synod park infrastructure stays unhardened per the T10/T23 canon guard). **Engineering:** steps sit on grade — not a terrain-stamp case; a modular stone-step unit thin-instanced along a slope spline, heightmap-snapped (same instancing pattern as creek rocks; step module joins T17 hardscape kit). SMR has real stone staircases — the georeferenced pipeline (T21) can place them at true locations.
- **Picnic grounds (provisional, from reference photos 2026-07-15):** a maintained clearing ringed by dark treeline — the mow line writ large — with a structure the open field lacks: **stone picnic tables as the only cover in an exposure zone** (low, solid, scattered at intervals; crossing the grounds becomes a table-to-table move). **Metal furniture = distraction terrain:** post grills and trash barrels are the loudest surface class in T18’s impact→noise grammar — a thrown rock on metal is a high-value clang, so the most exposed space is also the richest in acoustic tools; risk and reward co-located. **Pavilion** (roof, no walls) = the gentlest first “roofed exterior” acoustic test — a half-step toward the interiors schema (rain-on-roof, light reverb) without committing to it. **Hub adjacency (T16):** at real trailheads, picnic grounds sit beside the parking lot — this is the hub’s front porch, the transitional band between car-safety and forest; SMR’s real picnic areas are placeable via T21. Daylight-benign, night-liminal: human infrastructure without humans, never explained.
- **Dead fountain (provisional, prop):** defunct stone drinking fountain — spigot gone, hole punched in the brickwork. One object of material-conditions storytelling: the creek runs, civic water is dead. Landmark-scale prop, candidate anomaly seed; unexplained per the diegetic-discovery filter (deep-lore tie stays unhardened per canon guard).
- **Giant snag (provisional, prop, photo 8):** shattered standing stump, torso-plus height, overgrown base — the root ball’s standing cousin in the “is that a person” prop family: figure-adjacent silhouette, but *vertical*, so it doubles as usable cover and a landmark in exposure zones. Static (no loose limbs — the root ball owns the interactable role). Joins T17 deadfall queue.
- **Shed / comfort station (provisional, photo 10):** abandoned one-room stone restroom — fieldstone walls, board gable, corrugated roof with vent pipes, graffiti, being swallowed by undergrowth. **The missing rung in the interior acoustics ladder:** pavilion (roof, no walls) → shed (one room, hard stone acoustics) → mausoleum/mall/domicile — the smallest full interior, cheapest pressure test of the interiors schema. Storytelling: the “WOMEN” sign makes it dead civic plumbing (the fountain’s sibling); graffiti = first evidence of other people in the DTA woods; both unexplained. Horror grammar: a one-exit room is shelter and trap in one object — whether the door opens is a design lever, left open. Vent pipes = free diegetic sound source (wind-over-pipe tone). Shell joins T17 kit; placeable at real SMR location via T21/landmark manifest.
- **Open grove / parkland (provisional, zone type, photo 9):** full canopy *above*, mown-clear *below* — distinct from picnic grounds (no furniture, no cover objects) and from the mow line (shaded, not a boundary). Long ground-level sightlines, concealment only behind trunks; dappled, quiet, weirdly exposed. **Fourth band in the hub gradient:** lot → picnic grounds → open grove → true forest. As a T23 region it’s cheap: a canopy-on/undergrowth-off profile variant.
- **Landmark manifest (T21 pipeline proposal):** photographed features become placeable anchors via `landmarks.geojson` in `packages/geo` — `{ name, lat, lon, feature_ref }` per point, projected through the existing WGS84→UTM path (same as GPX validation). Photo EXIF is stripped by upload pipelines — coordinates come from originals (AirDrop/Files preserve EXIF) or transcribed from the Photos app info pane; for a handful of landmarks, hand transcription wins.
- **Reference photo set (2026-07-15, Dan, on-trail):** (1) backlit canopy w/ hero trunk — palette/chartreuse-clip reference; (2) creek bed — creek corridor spec above; (3) fern colony + mossy log — scatter authoring reference: ferns colonize in *clumps* (noise-driven patches, not uniform sprinkle; feeds T24 chunk scatter + T7 density authoring); (4) uprooted root ball — **landmark anchor + natural false-scare + interactable in one prop**: limbs-reaching-up silhouette reads as a figure at range (silhouette-first rule); place few, varied facing. **Limbs are loose and pushable (real-world observation, Dan 2026-07-15):** never self-animating — motion is player-caused only, which preserves the scare grammar (the “figure” moves *when you touch it*). Push → hand-rolled damped hinge rotation (no physics engine, per T18 rule; named constants for swing arc/damping) → creak/knock **noise event through T18’s impact→noise grammar** — so it’s simultaneously a deliberate distraction tool and an accidental-noise hazard when brushed while fleeing. Audible half queues behind T3 with the rest of T18; mesh + loose-limb rig joins T17 deadfall queue (limbs as separate bones — a natural Tier 1–2 rigging exercise); (5) stone trail staircase — random stairway spec above; modular step unit reference; (6–7) picnic grounds — stone table/bench kit, post grills, trash barrels, pavilion, dead fountain, stumps/boulders scatter; cover-layout and hub-adjacency reference; (8) giant snag — standing figure-silhouette prop; (9) open grove/parkland — canopy-over-mown zone reference; (10) abandoned stone comfort station — shed spec above; fieldstone/gable/roof-vent reference. **The photo-derived feature set (1–10) is the “SMR starter pack” — the hub spoke’s level-design brief.**
- **Engineering:** terrain stamps for overhangs (→ O3); concealment mask sampled in terrain-splat UV space, one fetch per tick, no raycasts; `FoliageSwayPlugin` (aspirational — not yet built, see T21's 2026-07-23 bullet) + translucency term (`dot(-lightDir, viewDir)`) for backlit ignition.
- **FPPOV lighting:** dapple via scrolling canopy-noise light cookie; god rays (`VolumetricLightScatteringPostProcess`, chartreuse-keyed); **diegetic exposure feedback — vignette strength tied to concealment, no HUD** (the audio-first north star's visual twin: the frame itself tells you if you're hidden); near-camera grass blades with near-blur for body presence.
- **Hero/scatter tree authoring tier (new, trail-adjacent detail need):** trail-adjacent trees (0–30m hero zone, FPPOV under canopy) need more detail than deep-stand scatter trees — same trunk-geometry-plus-alpha-cutout-leaf-card method as scatter trees, just authored at higher density with two additions: (1) bent normals via Blender's Data Transfer modifier, copying normals from a canopy-hull proxy onto the leaf cards so the canopy lights as one soft volume instead of noisy per-quad shading; (2) interior card fill on hero trees only — FPPOV walks under/through canopy constantly, and hollow-shell trees (cards facing outward, empty interior) are visibly wrong from below. Scatter-tier trees derive from the hero template by decimating cards and dropping interior fill — one authoring template, two output tiers, not two separate pipelines. No renderer change: confirmed compatible with existing thin instancing and (aspirationally — neither exists in code yet, see T21's 2026-07-23 bullet) T24's three-zone LOD and `FoliageSwayPlugin`. Alpha cutout only, never alpha blend (sorting). **POC step before original authoring:** source one reference tree (Poly Haven, CC0 — closest existing implementation of the bent-normal + card technique) to validate the Blender→glb→Babylon round trip and surface gotchas (does bent-normal data survive export/import intact? does the sourced asset expose the hollow-interior problem as expected?) before Dan builds an original hero-tree template. Owning doc named `hero-tree-poc-prompt-v1.md` — **never actually committed (see Doc inventory above); the POC work happened directly in code instead of waiting for the doc.** Quaternius CC0 lowpoly tree pack usable as cheap scatter-tier filler in trail-viewer (T21) in the meantime — not a technique reference, just placeholder density.
- **POC landed and exceeded scope (verified in code, 2026-07-22 — no THREADS entry existed for this until now):** five real Poly Haven `.glb` assets are loaded and thin-instanced in trail-viewer's hero zone — `tree-small-02` (the original 52k-tri validation target), plus `fir-sapling-medium`, `pine-sapling-medium`, `dead-tree-trunk-02`, `tree-stump-01` (`apps/trail-viewer/public/models/*`, `TreeAssetPreview.ts`, `HeroTreeInstances.ts`, `gltfLoader.ts`). **Real gotcha solved, not anticipated by the POC doc:** multi-material GLBs (trunk+leaf-card submeshes with different vertex attributes) can't go through `Mesh.MergeMeshes` (requires identical attributes across sources) — fixed by calling `thinInstanceAdd` on each submesh with the *same* matrix array instead, so every part renders at every location with no merge step. Per-instance scale jitter is independent per axis, bounded ±18% (same-species natural variation, wide enough to read as individual specimens, not wide enough to read as a different species). `HeroTreeInstances.ts`'s own header comment cites `docs/archive/THREADS-fold-in.260721.md` by name as its originating plan — that file is kept in the repo for this reason (see Doc inventory above), even though its content is now landed rather than pending. **This POC-tier hero scatter is explicitly not a replacement for `ThinInstanceTrees`' forest-wide scatter, which stays procedural** — two tiers, as designed, not a silent swap.
- **Trailside scatter extended to yellow-blazed OSM trails, 2026-07-22 (committed):** runs along the GPX track and yellow-blazed OSM trails now, not just the GPX track alone; "Trailside count" default/max bumped (30→60 default, 300→600 max) to cover the added corridor length. Confirmed via Dan's own playtest (steady 60fps on his hardware with the near-spawn grove + this combined).
- **Bulk/non-trail-adjacent forest tier, 2026-07-23 (committed):** direct response to Dan's own comparison ("the procedural trees look horrible compared to these new ones") — rather than reskinning `ThinInstanceTrees`' primitive cone/sphere geometry, a decimated real `tree_small_02` is scattered across the same `treePointsInRegion()` candidate pool `ThinInstanceTrees` draws from. **Partitioning, not duplication:** the region's point list splits into two disjoint slices — `[0, bulkCount)` get the real tree, `[bulkCount, treeCount)` stay procedural — so "# Trees" still means total count; "Bulk forest count" is how many of those are real instead of primitive. Own signal group (`bulkForestScatter.ts`) and its own three sliders, not reusing `treeScale`/`trailsideScale`, per the same separate-sliders principle as the trailside cluster. Default count 200, conservative starting point. **Dan correction, later 2026-07-23:** leaf-card loss is acceptable in this distant/dense tier. The earlier 7,660-triangle visual floor was therefore too conservative; keep experimenting with more aggressive decimation or leaf-stripped variants here while preserving the full assets for hero/trailside clusters.
- **Compound geographic locations + first Dissonance Boulevard block, 2026-07-23:** `locations.json` now supports a `compound` alongside point props: one lat/long anchor scopes a rotated local meter grid with explicit/repeated placements, so roads, buildings, and street furniture move as one authored location without assigning fake landmark coordinates to every module. First block uses Quaternius's CC0 Downtown City MegaKit: an ~84 m two-lane street, paired sidewalks, six thin-instanced building placements across three authored types, bollard rows, and planters. Assets load once per type via `CompositeLocations.ts`; imported texture copies are capped at 1024 px for browser iteration. This is a layout experiment, not yet the T13 shipping boulevard: terrain grading/collision, catenary wires, surveillance patrols, Milo's interior, and the wireframe-over-mass treatment remain separate work.
- **Boulevard block polish + first cross street, 2026-07-24 (committed):** three-part follow-up, Dan requested all three "in order": (1) **polish** — dropped the leftover point-props (`rocks`/`trees`) that were jittering onto the paved street from the pre-compound version of this location; added a repeated `street-lamp` row (13 total) down both sidewalks via a new `PROCEDURAL_ASSETS` path in `CompositeLocations.ts` (non-glTF compound assets, same repeat-grid placement math as the city kit's modules — `LocationProps.ts`'s `buildStreetLamp` is now exported for this); swapped a hand-rolled UV-cropped crosswalk decal plane for the kit's real `Decal_Crosswalk.gltf` after discovering a fuller local copy of the same CC0 "Standard" package (`D:/dan/code/dissonance-related/Downtown City MegaKit[Standard]`) with far more modules than the 7 first copied in — real asset looks correct and needed no UV-crop guesswork. (2) **expand footprint** — pulled 6 more modules from that same local CC0 copy (`Street_TIntersection`, `Sidewalk_Corner_Flat_3m`, `Decal_Crosswalk`/`_Wide`, `Prop_ManholeCover`, `Prop_Drain`); split the main street's repeat to seat a T-intersection mid-block with a short cross street (3 more `street-2lane` segments + sidewalks + one more `building-small`) branching off. Verified structurally via `[CompositeLocations]` placement-count console logs (all counts match the authored repeat math, zero unknown-asset warnings) and the crosswalk visually in-browser; the intersection's own rotation/corner treatment is a first pass, not yet eyeballed up close — corner sidewalk pieces weren't wired in this pass. (3) **terrain integration** — not started; confirmed the real problem this pass exists to solve while walking the block: each module ground-snaps independently (`getHeightAt` per placement, no shared grading), so sidewalk curbs on this sloped DEM site cut a hard visible cliff against the surrounding dirt terrain instead of sitting flush.
- **Terrain integration + footprint growth, 2026-07-24 (committed):** picked up items (1) and (3) from the entry above at Dan's direction — explicitly not (2)'s corner-sidewalk/intersection-eyeball item, which stays open. **Shared per-compound grading:** `CompositeLocations.ts` no longer calls `getHeightAt` independently per placement; each compound now fits one linear tangent-plane to the DEM at its anchor (3 samples — center + 15m along world X/Z) and every placement in that compound reads its ground height off that shared plane, so adjacent modules (sidewalk-to-sidewalk, sidewalk-to-building) stop drifting with the DEM's own per-vertex noise. Simplified `ExpandedPlacement` to carry fully-resolved world `x`/`z`/`groundY` instead of anchor-relative offsets resolved later — removed the old "merge placements from different anchors under one zero-anchor" workaround entirely (net reduction in indirection, not just an add). **Grade pad:** a new procedural mesh (`buildGradePad`) per compound — a flat-but-tilted quad sized to the compound's placement bounding box + 3m margin, extruded down to a generously buried base (30 units) — visually absorbs the seam between the compound's shared grade and the DEM's real, unaltered slope around it; deliberately scoped narrower than a general T23 terrain-stamp system (still gated on the interiors schema pass per T23's own note) — this is a boulevard-specific fix, not that system arriving early. Doesn't touch `HeightmapTerrain`/`packages/world` at all. **Footprint growth:** capped the cross street's dead end with a `building-large` terminus (first pass on rotation, not eyeballed) and mirrored its one-sided building/lamp row onto the north side (`building-medium` + a second `street-lamp` row) so the side street reads as a real two-sided block instead of trailing into empty pavement. Verified via `tsc --noEmit` (clean), `[CompositeLocations]` console counts (all match the authored placement math, zero unknown-asset warnings), and an in-browser look via chrome-devtools automation — real pointer-lock mouse-look isn't obtainable from CDP-issued input in this environment, so the check used descend/backward flight (Space/S dispatched as synthetic `KeyboardEvent`s, which movement — but not the pointer-lock-gated mouse-look — reads regardless of trust) rather than a normal walkthrough; screenshots show a contiguous graded surface meeting the sidewalks/buildings with no visible floating gap or cliff, but a manual walk-through pass is still worth doing for the final subjective call, especially on the new terminus building's rotation.
- **Boulevard build-method review + fixes, 2026-07-24 (committed):** Dan asked for an evaluation of the compound-object build method (since it was written by a different agent/session) before continuing. Review found the core architecture sound (same "hand-authored JSON, interpreted at load" pattern as `ThinInstanceTrees`/`HeroTreeInstances`/`LocationProps`, batched by asset type across compounds, consistent with precedent) but two real bugs in the grading work from the entry above, both fixed: (1) **isotropic scale** — `CompositeLocations.ts` folded `horizontalScale` into all three axes of every placement's scale (`Vector3(scale,scale,scale)`), so buildings grew physically *taller* purely because the world's H-scale slider went up, with no relationship to `verticalExaggeration` and no separate scale of their own — unlike every sibling scatter system (`HeroTreeInstances.ts` keeps `Vector3(sxz,sy,sxz)`, driven by independent H-scale/V-scale sliders). Fixed by splitting into `scaleXZ` (× `horizontalScale`) and `scaleY` (× `verticalExaggeration`), threaded through a new `verticalExaggeration` parameter on `loadCompositeLocations` (both `main.tsx` call sites updated; already re-fires on vExag changes via the existing combined `rebuildWorld`/`onScaleCommit` path, no new wiring needed). (2) **grading constants in the wrong space** — the grading-plane sample distance and pad margin were added directly to *rendered* (already-hScale'd) coordinates, but `getHeightAt` divides by `horizontalScale` before sampling the real DEM, so the real-world distance was only correct at `hScale=1` and shrank as hScale rose. Fixed together with a second, related gap (the plane was only sampled ±15 units from the anchor regardless of how far the compound's own placements actually extend — asymmetric after this session's cross-street growth) by restructuring: placements now resolve position first, bounds are computed from them, and the grading plane is fit by *central difference across the compound's own bounding half-extent* (with a real-meter floor for degenerate/tiny compounds) — this is correct in real meters at any hScale and automatically brackets whatever footprint actually exists, no fixed magic distance. Smaller fixes bundled in: a material leak on every hScale/vExag rebuild (`gradePads`/`proceduralMeshes` now `dispose(false, true)`), and a z-fighting risk between the grade pad's top and the real terrain it sits almost exactly at (small downward top-inset). Verified via clean `tsc --noEmit` and an in-browser check specifically at a non-1 hScale (a stale `localStorage` save happened to restore "dissonance environment test 2"'s `hScale=3.5`/`vExag=7.5` — good, since that's exactly the scenario the old isotropic-scale bug needed to be visible in; buildings read at normal proportions, not stretched). No schema-validation gap was "fixed" — flagged in review as an accepted tradeoff (hand-edited JSON, no build step), not a defect.
- **Terrain shading fix (general, not boulevard-specific), 2026-07-24 (committed):** Dan separately flagged the ground itself looking "striated, like a skin on a mesh." Root-caused in `HeightmapTerrain.ts`: Babylon's `CreateGround` (confirmed against its own `groundBuilder.js`) splits *every* quad along the same diagonal, and normals were derived from that coarse, uniformly-grained mesh (`VertexData.ComputeNormals`) rather than the DEM's own shape — a uniform-direction "grain" that verticalExaggeration (10–16× at Level 1) amplifies into visible striping, under a real photo texture that doesn't match the low-poly geometry underneath it (the "skin" look). Two fixes, no added triangles: (1) checkerboard the triangulation — quads at odd `(row+col)` now split along the opposite diagonal, derived by reflecting the main-diagonal index formula and re-reversing to preserve winding, confirmed correct in-browser (no dark/inverted-normal facets) rather than trusted from the hand derivation alone; (2) replaced mesh-derived normals with normals sampled directly from the DEM's own continuous bilinear gradient (reusing the existing `SLOPE_SAMPLE_OFFSET_METERS` finite-difference distance, converted into rendered space via `verticalExaggeration/horizontalScale`) — shading now reflects the real terrain's shape at full resolution instead of the mesh's coarse facets. Checked at Level 1's extreme end (`hScale=1.5`, `vExag=16`, "a view of the mountain") in daylight: slopes now read as natural ridge/erosion texture rather than a single repeating diagonal pattern. A further lever (raising `gridResolution` past its current ~8m/quad, which roughly matches the DEM's own ~7.7m/pixel native resolution) was identified but deliberately not applied — it's a real performance/quality tradeoff, not a strict improvement like the two changes above, and wasn't asked for.
- **Removed ThinInstanceTrees + hero grove, 2026-07-24 (committed):** Dan asked to drop both tiers (and their Dev HUD sliders) entirely, no replacement. `ThinInstanceTrees` (the procedural cone/sphere forest-wide scatter, `packages/world/src/ThinInstanceTrees.ts`) is deleted outright — nothing else in the repo imported the class itself, only its `TreePoint` type, which `ForestFire.ts` now owns directly (it was the last real consumer) and re-exports through `packages/world`'s barrel in its place. The near-spawn hero grove (`heroZonePositions`/`heroClusters`/`repositionHeroClusters`, the fixed-lat/lon annulus of 5 Poly Haven species) is gone from `main.tsx`; `HERO_ASSETS`/`HERO_WEIGHT_TOTAL` survive since the **trailside** tier reuses them as per-species mix weights along the GPX/OSM corridor — confirmed that tier, bulk forest, and `CompositeLocations` all still load cleanly with no dangling references. `treePoints`/`treeRegionRadius`/`treePointsInRegion()` also survive (ForestFire still consumes the region-filtered candidate pool; `treeRegionRadiusMax` still sizes the Bulk-forest-radius slider) — only their own dedicated HUD section and the "Thin-instance tree radius" slider are gone, so that pool's radius is fixed for the session instead of live-tunable. Also deleted as now-fully-orphaned: `ui/TreeCountRow.tsx`, `state/treeScale.ts`, the "Thin trees" visibility toggle (`VisibilityToggles.tsx`/`state/visibility.ts`). Verified via clean `tsc --noEmit` across `trail-viewer`, `packages/world`, and `dont-turn-around` (untouched — `ThinInstanceTrees` was trail-viewer-only, no DTA import ever existed), plus an in-browser reload: bulk forest, trailside, and the boulevard compound all still log their expected counts with zero console errors.
- **Bulk forest understory mix (prototype, 2026-07-24, committed):** with the hero grove gone, bulk forest is now the sole non-trail-adjacent forest tier — Dan asked how to get it more visual variety, without necessarily doing new asset authoring. Recommendation given: reuse the same weighted multi-species pattern `HERO_ASSETS`/trailside already use (independent random draw per species, sized as a fraction of a shared count), but point it at assets that already exist at scatter-tier tri budgets rather than full-detail saplings, since bulk forest runs at 10x+ hero-grove's instance count. Prototyped: `tree_stump_01`/`dead_tree_trunk_02` (already-shipped, already-decimated-enough understory props) mixed in at small fixed fractions of `bulkForestScale.count.value` (3%/2%) via a new `BULK_UNDERSTORY_ASSETS` array, sharing `bulkForestRadius`'s own candidate generator (`createBulkEligibleCandidatePositions`) and `bulkForestScale`'s existing H/V-scale sliders — no new HUD controls. Verified via clean `tsc --noEmit` and console counts matching the fraction math exactly (200-count default → 6 stumps/4 trunks). Deliberately conservative fractions, easy to retune. **Bug caught on first playtest (Dan: fallen tree floating / sitting on top of another forest object) and fixed same pass:** `createBulkEligibleCandidatePositions`'s seed depended only on `radius`, not on which species called it — every call at the same `bulkForestRadius` (dominant tree + both understory species) drew the *identical* deterministic sequence, so the understory props' leading positions were landing exactly on top of the dominant tree's (and each other's), not merely close by. Fixed with a per-species seed salt (FNV-1a hash of the asset's label, XORed into the seed) so each species decorrelates from the others while staying reproducible per radius (needed for scale-stability across rebuilds). **Not yet addressed:** giving the *dominant* scatter tree itself more detail (e.g. restoring leaf cards on `tree_small_02_scatter`) is a separate, competing lever — real FPS cost at bulk's instance counts, needs its own benchmark before deciding between "more detail on one asset" vs. "more species mixed in," or both.
- **Wind volume now drives foliage sway too, 2026-07-24 (committed):** Dan noticed wind audio and foliage sway both tracked `weatherSystem`'s live `windIntensity`, but only the audio side (`ambientAudio.setWeatherIntensity`) also ran it through the user-facing "Wind vol" slider — dragging that slider changed what you heard with no visible change in how hard the trees moved. Fixed with a small `FoliageSwaySource` adapter (`visualWindSource`) that multiplies `weatherSystem.getWindIntensity()` by `audio.windVolume.value` at read time, passed to all three remaining `loadHeroTreeInstances` call sites (bulk forest's dominant tree + understory mix, trailside) in place of the raw `weatherSystem`. `WeatherSystem` itself is untouched — the adapter lives in `main.tsx` only, so its own gust/ramp dynamics and `getMaskLevel()` stay unaffected by a user-facing volume preference. Verified via clean `tsc --noEmit` and a reload with matching console counts (shader itself — `FoliageSwayPlugin` — untouched, only which object it reads from changed).
- **Status:** direction set; lands as profile data + shader extensions when world assembly (T23) opens

### T23 — Region system / continuous world 🆕
- **Status:** design committed — implementation after T3/T4; interiors schema pass is a prerequisite for interior regions
- **Thesis:** one continuous world (SMR-scale TrailViewer terrain), environments as *regions selected by profile*, not levels selected by scene load. The landscape targets (T6) become zones in one space, not separate scenes.
- **The one new engine seam:** a spatial query — "which profile governs here" — via zone volumes or a splat-space region mask, feeding `applyProfile()`. Boundary crossing = lerped profile transition. **Shares lerp machinery with T5** (reactive environment): spatial trigger and detection trigger drive the same seam. Route both through one transition system, one session.
- **DTA-onto-TrailViewer work items:**
  - Pursuer consumes real terrain: `getHeightAt` locomotion + tile-based navmesh bake (O2 — now decided-required)
  - Concealment mask (splat-UV, one fetch/tick) bridges TrailViewer foliage → detection logic
  - Collectible rings (T15/T16) = authoring pass on real trailhead topology
  - **Environment/atmosphere side of the same migration (new, 2026-07-24):** `dta-environment-port-prompt-v1.md` (`docs/plans/`) audits the gap between DTA's `Terrain`/`ForestGenerator`/`ExperienceProfile`/`RunProfile` and trail-viewer's DEM/scale-tuning/tiered-forest/atmosphere systems, using the `dissonance environment test 1` view as its worked example. Five open questions gate any implementation session (schema-or-stay-a-snapshot chief among them) — see the doc and T21's matching bullet above.
- **Interior/hardscape regions:** terrain stamps (O3) for mall/mausoleum/domicile; flattened patches for parking lot/urban-edge. Gated on interiors schema pass.
- **Density budgets:** per-region scatter budgets; likely tile-based content loading (rides the O2/O3 tile decision).
- **T7 substrate — RESOLVED 2026-07-27 (Dan):** `instance-placement-prompt-v1.md` and `scatter-placement-prompt-v1.md` both proposed an ambitious, **data-first** placement architecture for T7/T23 — offline-generated manifests (`scatter.json`, `powerlines.json`), an anonymous thin-instance layer plus a sparse identified-overlay layer for interactables/photos/lore, cell-based LOD with hysteresis, and a QGIS-authored zone-field system driving fog/grading/`glitchIntensity` as one blended sampler shared by visuals, audio events, and non-visual actuators. Core stated principle: **"the runtime never scatters, it reads a committed manifest."** This contradicted what's actually shipped — `ThinInstanceTrees`, `HeroTreeInstances`, `bulkForestScatter`/`trailsideScatter`, and trail-viewer's `CompositeLocations` (boulevard buildings) all scatter/place procedurally at runtime (seeded, but tunable live via HUD sliders — H-scale/V-exag/radius/count/window-tint all rebuild this way), not from a committed manifest; no `data/placement/scatter.json` or `powerlines.json` ever existed, and no zone-field/`glitchIntensity` system exists. **Decision:** the runtime/data-hybrid approach (what `CompositeLocations` already does) stays the standing architecture for placed content — it's what every session has actually shipped, and the project's live-slider HUD-tuning workflow depends on runtime regeneration, not a frozen bake. Both docs are marked **superseded on their offline-manifest core premise** — reference-only from here, do not treat either as a session brief as originally written. Two ideas are salvaged out of them as their own smaller threads rather than adopted wholesale: **T26** (a minimal identified-overlay — stable `{id, position-or-ref, tags}` entries for landmarks/compass-targets/interactables, without the full anonymous/identified/cell-LOD/zone-field megastructure) and **T27** (zone-field atmosphere blending — spatially-varying fog/grading by player position, independent of how scatter itself works; T25 already wants this). Cell-based LOD/hysteresis stays with T24, unaffected by this decision. Surfaced via a rural-infrastructure feature request (`@dissonance/rural-infrastructure`) that would otherwise have proposed a third, larger version of the same offline-manifest architecture.
- **Canon guard:** continuous world = *shared engineering world*, not confirmed shared fiction — see T10 note. Cemetery remains the designed seam.
- **Owning doc:** none yet — needs `region-system-prompt-v1.md`
- **Ties:** T1/T2 (profiles are the mechanism), T5 (shared transition seam), T6 (targets become regions), T7 (world assembly, not population), T15/T16 (rings/hub), T21 (terrain substrate), O2/O3.

### T24 — Forest performance & chunking 🆕
- **Status:** handoff ready — parallel-safe now (app-local to trail-viewer, no `@dta/audio` or package extraction)
- **Scope:** thin-instance batching, chunk-based activation (32/64m, named constant), distance LOD extending the proven three-zone scheme, impostor/silhouette far tiers, near-player-only pooled collision proxies (cylinders/capsules, named radius), fog/darkness as deliberate performance tools, Dev HUD debug counters (extends T1/T2, never forks).
- **Origin:** Dan’s ChatGPT-generated engineering prompt, conformed to house rules: Phase 0 audit gate added (P2 — `ThinInstanceTrees` already exists; three-zone LOD and `FoliageSwayPlugin` do not, per `generation-systems-audit.md` and this session's repo search (T21, 2026-07-23) respectively — the P2 audit's premise was wrong on two of three counts; audit prevents rebuilding them, P1); target named as trail-viewer (P8 correction); PS1/dithered aesthetics stripped (T22 owns visual direction — T24 has no player-lighting scope of its own); all tunables named-constants-now, `profiles/`-later (conflict rule 2). *(Correction, Dan, 2026-07-15: this line previously also stripped flashlight aesthetics citing "T15/P5 forbid flashlight" — wrong; P5's actual text is firefly-color exclusivity only, not a flashlight ban. The current `dont-turn-around` flashlight system (`SpotLight`, cone-based detection via `isPointIlluminated`/`getFlashlightPressure`, phone-pickup unlock) is good code — keep it in both the current app and its museum-piece copy, and it's a reuse candidate when DTA mechanics map onto trail-viewer.)*
- **T23 alignment:** chunk metadata (biome id, density, trail proximity) ≈ the region mask — chunks designed as T23’s future spatial substrate, not a parallel system.
- **Concurrency advisory (Dan, 2026-07-15):** T24 and general T21 dev share `apps/trail-viewer`. Default **serial**. If concurrent, T24 is fenced to forest modules only (chunking, tree/scatter, LOD, collision proxies, forest HUD counters) — no traversal/water/sky/snapshot/settings edits; out-of-fence needs surface to Dan instead of crossing. (Advisory recorded in the owning prompt’s header.)
- **Owning doc:** `forest-performance-prompt-v1.md`
- **Ties:** T21 (target app), T1/T2 (HUD counters), T22 (aesthetic boundary), T23 (chunk→region), T17 (tree/rock/deadfall variants)
- **v9.20 addendum fold-in (reconciled v9.43):** T24 also owns the addendum's chunk residency, chunk-level frustum/cull policy, billboard impostor spike, fog-horizon transition cover, and the rule that chunks own instance buffers while hero meshes remain globally resident. Fog, LOD, culling, and streaming are independent mechanisms sharing a distance axis; `cullRadius` must not become an alias for `streamRadius`. The addendum's old “T26” label is superseded.

### T25 — Atmosphere grading + time-of-day (dawn look) 🆕
- **Status:** split. The precipitation/storm foundation landed 2026-07-30; the dawn grading/profile-compose work below remains design-only and retains its T1 schema gate.
- **Precipitation/storm foundation landed:** shared weather mode/state now covers clear, windy, rain, snow, and storm; the World app has camera-relative rain/snow, weather fog/noise/intensity outputs, a Tone.js rain bed, and a seeded flash → delay → thunder scheduler. World HUD selection and persistence/export use the same `WeatherSystem`; T31's authored strike commands that system rather than forking weather. Known follow-ups: detection/hearing consumers do not yet use precipitation noise or snow cover; thunder is not yet consumed as a masking window; night-vision penalty, distance cull/horizon treatment, full profile/HUD exposure of tuning constants, and the final post-process composition remain open.
- **Origin:** photo reference, side session 2026-07-15 — hazy sunrise through backlit canopy: blown-out sun, magenta/pink cast (purple shadows → cream highlights), pink-tinted distance haze flattening ridgelines, faint diagonal rays, teal lens-flare ghost. **The look is camera artifact, not scene content — it lives in the post stack.**
- **Core principle (route through T1):** all of it is data. Extend the profile schema with atmosphere/grade parameters, no new renderer code paths: bloom (threshold/weight/scale); color grade as **ColorCurves params** (shadow/mid/highlight hue+sat) — chosen over baked LUTs so time-of-day can interpolate; fog color/density (dawn variant tints the existing field); volumetric scattering (intensity, sun anchor) — **unify with T22’s god-ray aspiration, don’t duplicate**; lens flare (enable, ghost color/count) — the “camera, not render” tell.
- **LUT both/and (Dan, confirmed):** optional static LUT slot for fixed-mood, non-interpolating contexts (interiors: mausoleum, domicile) — **mutually exclusive with curve-lerp per profile**; a LUT in a time-interpolating profile is a loud load-time validation error (same spirit as clip-name validation).
- **The interpolation seam — now three drivers, one system:** time-of-day = lerp between keyed atmosphere profiles (dawn/day/dusk/night per biome). Same lerp machinery as T5 (detection state) and T23 (region crossing). **Build the transition/compose system once, three drivers: region × clock × detection.** Proposed compose rule (provisional, needs sign-off → O9): region selects the profile family, clock interpolates its keyed variants (the base), detection-state grading composes on top as a weighted overlay — so dawn and hunted never fight; hunted modifies whatever hour it is. Named blend weights, defined once.
- **Prior art (P1):** trail-viewer already owns `setTimeOfDay(hour)` (sun/ambient/sky/stars together, T21). T25 layers grading onto the existing cycle — it does not build a cycle.
- **Dev HUD (T1/T2):** all params exposed and export-to-JSON like any profile data; dawn look authored live, committed as data.
- **Ties:** T1 (schema extension — gate), T2 (authoring), T5/T23 (shared seam), T21 (existing cycle), T22 (god-ray unification, palette boundary)

### T26 — Identified-overlay landmark layer 🆕
- **Status:** new, unscoped — salvaged from the parked `instance-placement-prompt-v1.md` (see T23's "T7 substrate" resolution note) rather than adopted as part of a full offline-manifest architecture.
- **Scope:** a minimal, sparse layer giving stable identity to specific instances/positions that need it — landmarks, compass targets (feeds T-navigation's `CompassReading`/`NearbyLandmark`), interactables, lore-tagged props — without requiring the anonymous-scatter layer, cell-based LOD, or zone-field system the original doc bundled it with. Entries reference either a runtime-scattered instance (by stable key, not raw array index — reseed-safety needs a real key, not position-in-array) or self-place explicitly (matching how `CompositeLocations`/`locations.json` already place buildings).
- **Not this thread:** offline manifest generation, cell-based LOD/hysteresis (T24's), zone-field blending (T27's).
- **Ties:** T7 (world population — this is its stable-identity substrate), T21 (trail-viewer's existing `locations.json` pattern is the closest prior art), rural-infrastructure landmark registration (compass targets, discoverable structures).
- **Owning doc:** none yet.
- **Manifest clarification (v9.43 addendum fold-in):** this is the home for the addendum's World Feature Manifest, narrowed to match T23's runtime/data-hybrid decision. Stable identified features support placement, saves, replication, compass targets, and deterministic regeneration; anonymous scatter remains runtime-generated. Geographic entries store WGS84 as authority and derive rendered world position rather than persisting two independently editable coordinate truths.

### T27 — Zone-field atmosphere blending 🆕
- **Status:** new, unscoped — salvaged from the parked `instance-placement-prompt-v1.md`; independent of T26 and of how scatter/placement itself works.
- **Scope:** polygons (or simpler shapes to start) that the player position samples every frame, blending fog color/density/grading and other atmosphere params by proximity/weight rather than trail-viewer's current single flat global value per field. Writes through the **existing** fog/atmosphere seam (`atmosphere.fogColor`/`fogDensity`/etc. in trail-viewer today) — no second atmosphere path. T25 (dawn-look grading) already wants exactly this blending mechanism for its region×clock×detection compose rule; build once, shared.
- **Not this thread:** offline scatter manifests, cell-based LOD, `glitchIntensity`/non-visual actuator channels (interesting from the original doc, but a further extension once basic zone blending itself lands).
- **Ties:** T25 (shares the blend/compose machinery), T23 (region system is the eventual authoring surface for zones), T5 (reactive environment shares this lerp seam).
- **Owning doc:** none yet.
- **Spatial-distortion extension (v9.43 addendum fold-in):** after atmosphere-only sampling proves the seam, zone profiles may also drive horizontal/vertical perceived scale, color grading, and ambient audio. Synod Capital is the first proposed radial field: vertical building distortion peaks at the center and eases continuously to normal at the suburbs. Canonical geo/navigation/save coordinates remain undistorted in the first slice; authoritative geometry/physics deformation is gated by O12. The addendum's old “T27” communications label is superseded.

### T28 — Rural infrastructure backlog 🆕
- **Status:** new, unscoped backlog — nothing built except the power-line/utility-corridor slice (shipped 2026-07-27, see T21's matching bullet above and T7-substrate's resolution note); **treat power lines as done, not part of this backlog.**
- **Origin:** a large `@dissonance/rural-infrastructure` feature-request prompt (2026-07-27) proposing eight feature categories, a new package on the scale of the rest of `packages/` combined, an offline asset-provenance/LOD pipeline, and a full test framework in one pass. Repository assessment found the prompt's core assumptions didn't match reality — no `EnvironmentProfile`/`applyProfile()`/`locationBuilder` anywhere in the repo (confirmed via search), no test runner at all (confirmed — `CLAUDE.md` already says so), and the placement-architecture question it would have re-raised a third time was already open (see T7-substrate, resolved same session). Scope was cut to the one piece with an obvious, unblocked path — power lines, built directly against `CompositeLocations`/`LocationProps` per the T7-substrate decision. The other seven categories are recorded here **so the ideas aren't lost**, not because any of them are scoped or ready to build:
  - **Farm silo** — landmark/hero-location, modular/procedural (cylinder body + conical roof + ladder/catwalk); variants include a Synod-retrofitted "resonator silo."
  - **Acoustic windmill** — reads as a plausible farm/energy windmill; secretly a large-scale sound machine. Needs a `BehaviorProfile`-style split of visual blade rotation vs. mechanical/acoustic state vs. Synod activation state; multiple units in one field should support a shared synchronization group (matching/drifting phase, beating against each other) — this is really a T20 (acoustic world systems) feature wearing a windmill's silhouette, not primarily a visual-system one.
  - **Water tower** — landmark, modular (tank/legs/cross-bracing/ladder), compass target + climbable hero variant, Synod comms-retrofit variant.
  - **Self-storage facility** — hero location, modular unit rows + office + gate, per-unit narrative/discovery state (same environmental-storytelling shape T22 already uses for the shed/fountain/picnic props — one story profile per prop, not hardcoded text), streamed/deferred unit interiors.
  - **Regional airport** — hero location, runway/taxiway/hangar/control-tower, broad vegetation-exclusion footprint; the single largest build in this backlog by a wide margin.
  - **Two-lane rural highway** — should extend the existing OSM/GPX polyline-drawing convention (`buildPolylineMeshes` in `main.tsx`) rather than invent a new road system; needs vegetation exclusion plus intersection/driveway hooks.
  - **Physical compass** — **not greenfield.** `packages/navigation` already has real code for this: `CompassReading`/`MapPlacard`/`NearbyLandmark` types (in `@dissonance/shared-types`) and working `bearingBetween`/`distanceBetween`/`createMapPlacard` functions. A real compass build starts there, not from scratch. The "compass deviates near active infrastructure" idea is a natural first consumer of T27's zone-field blending once that lands, rather than its own bespoke interference system.
- **Ties:** T26 (identified-overlay landmark layer — every landmark-scale item above needs this for stable identity/compass registration, none of them have it yet), T27 (zone-field atmosphere, specifically compass-interference zones), T20 (acoustic windmill's real owning thread), T17 (asset queue — silo/water-tower/etc. join the same "procedural placeholder now, real asset later" queue as root-ball/creek-rocks/shed-shell/etc.), T21 (highway extends its existing polyline convention).
- **Owning doc:** none yet.

### T29 — Diegetic communications / terminal layer 🆕
- **Status:** experimental; offline vertical slice before any networking.
- **Scope:** a portable green-screen terminal rendered in Preact above Babylon, mechanically docked to T26-identified world features. First implementation is local fixture data plus an explicit `far → available → docking → docked → undocking` state machine and clean input-focus handoff. Runtime dialogue follows Simulation → Context → Provider → Validation → Authorized Dialogue; providers never own world state.
- **Authority split:** T29 owns the client interaction, docking, terminal presentation, context adapters, and the Synod Scrambler contract. T11 owns network transport, sessions, replication, factions, provenance, and server authority. WebSockets precede WebRTC, matching the older monorepo plan; media transport is not part of the first slice.
- **Infrastructure resonance:** terminal docks can later act as acoustic injection points; fiction belongs to T10, acoustic behavior to T20, and physical landmark variants to T28.
- **MCP horizon:** `getNpcContext`, `inspectSignalRoute`, `requestMessageSend`, and `getFactionMemory` are authoring/inspection tools only. Requests still cross the same authority/validation boundary as a game client.
- **Presentation framework resolved (2026-07-31):** Preact + Signals, SVG-forward, composited over Babylon. Terminals observe game state through a thin signal bridge and submit intent through commands; they never reach into engine internals.
- **T31 control extension (gated):** the portable terminal is the future addressing/control and recorded-feed review surface—not a handheld flight stick. Current lean is misuse of Milo's legitimate authorization, still awaiting narrative sign-off. Live Lineglass sight additionally requires the scarce receiver camera. Repair-queue addressing, viewpoint-binding/piloting, and disabled-drone-as-suppressed-transmission all remain design-only; none are prerequisites for the landed acquisition v1.
- **Owning doc:** `plans/addendum-reconciliation-implementation-v1.md`.
- **Ties:** T10 (lore/factions), T11 (network authority), T20 (acoustic infrastructure), T26 (stable dock identity), T28 (physical infrastructure).

### T30 — Instanced material pipeline 🆕
Turn reference-art material swatches into tileable PBR textures + a
thin-instance per-instance variation shader, plus a reusable UV-scrolled
emissive material for machine-readout surfaces.
- **Handoff:** `instanced-material-pipeline-prompt-v1.md`. Constants/tradeoffs:
  `instanced-material-pipeline-constants.md` (both in `docs/dissonance/`).
- **Landed (first pass):** new package `packages/materials`
  (`@dissonance/materials`) — `ScatterVariationMaterialPlugin` (per-thin-instance
  hue/value jitter via an instance buffer, `MaterialPluginBase` sibling to
  `@dissonance/world`'s `FoliageSwayPlugin`), `createScatterMaterial`,
  `EmissiveDataPatternMaterial` (UV-scrolled, standalone, no device logic
  wired in). Verified live in a real browser (`apps/materials-demo`, dev-only,
  not deployed): 144 thin instances, visible per-instance variation, both
  emissive planes scrolling, no shader errors, 60fps — the
  `CUSTOM_FRAGMENT_UPDATE_ALBEDO` injection point was unverified guesswork
  going in, confirmed working now.
- **Package home resolved (was open decision #1 in the handoff doc):** Dan
  called `packages/materials` over app-scoped, since the instance-builder
  consumers (`ForestGenerator`, `apps/world`'s `CompositeLocations`/
  `HeroTreeInstances`/etc.) already live under `packages/world`/`apps/world`,
  not DTA — **and DTA is explicitly out of scope for any new work
  permanently**, not just this thread (it's the frozen museum exhibit; see
  CLAUDE.md).
- **Deferred, not resolved:**
  1. Only one of the doc's five named swatch families got baked (a
     worn-leather/tooled-leather patch standing in for "frayed
     fabric"/"charcoal weave") — rust, cracked plastic, and duct-tape/wrap
     only exist as ~65×65px thumbnails in the source sheet, too low-res to
     bake honestly. Needs dedicated higher-res crops before those three can
     be built.
  2. Seamless tiling uses an "offset and heal" technique (quadrant swap +
     blurred center seam) — no content-aware seamless-texture tool was
     available. A visible-on-close-inspection seam remains; a mirror-quad
     approach was tried first and rejected (obvious kaleidoscope artifact).
  3. Atlas vs. single-tile-plus-variation (handoff doc's open decision #2):
     not decided, moot for now — only one swatch family exists, so there's
     no second tile to index. Revisit once a second family lands.
  4. **Asset licensing not independently confirmed** (Phase 0 audit's
     explicit "flag if uncertain, do not assume"): the source images are
     AI-generated project-original art (the "Echo-17" character sheet) Dan
     supplied directly, not a verified-clean third-party asset. Each baked
     texture's `ASSET-LICENSE.txt` (under `apps/world/public/textures/echo17-*/`)
     carries this caveat explicitly rather than asserting it's cleared.
- **Ties:** shares the thin-instance/`MaterialPluginBase` pattern with T21/T24
  (`@dissonance/world`'s forest instancing, `FoliageSwayPlugin`); its emissive
  material is a substrate for T29's terminal layer and any future
  Vane/Lineglass-style readout surface, but embeds no device state logic
  itself (same "substrate, not the device" boundary the handoff doc set).

### T31 — Interference verb / captured patrol drone (emitter)
- **Status:** v1 acquisition complete; mounted-emitter interference/control verb deferred.
- **Landed foundation:** an app-local Boulevard patrol drone with stable authored ID, authored patrol path, visible read cone, and per-instance inert override. It does not change shared pursuer behavior. The same slice established the named Tone.js audio buses and ambient ducking seam required by D1.
- **Landed acquisition beat:** a Milo-proximity- and LOS-hold gate selects a seeded utility-pole anchor, commands T25's storm weather, flash, and thunder, leaves the selected drone inert, and exposes proximity recovery. The beat does **not** depend on workshop discovery. Witness and recovery state survive reload; recovery grants stable hardware IDs for the chassis and emitter. Dev HUD diagnostics expose gate state and its live blocker.
- **Completion (2026-07-30):** all tuning loads from validated `public/data/strike-profile.json`, is editable and losslessly importable/exportable through Dev Lineglass, and drives the gate, flash/clap, recovery range, and drone settle animation without duplicate constants. Durable WorldSave strike state preserves run seed, selected anchor, wind-up, and downed position across reloads. Vitest covers dry/wet entry, LOS hold, one-shot, seed determinism, profile round-trip, and recovery boundary.
- **Live QA follow-up:** repeat the full-speed fly-past, visible strike, audio, and HUD-progression pass on stronger hardware. The authoring machine did not visibly show the strike/HUD advance during one pass, likely due to LOS/timing; this remains an observational tuning check, not an incomplete deterministic gate or a workshop prerequisite.
- **V2 boundary:** emitter construction/mounting, `applyDisruption`, wear/maintenance, animal mapping, piloting/viewpoint binding, terminal addressing, receiver-camera live feed, repair-pipeline exploitation, recharge/costs, and loss conditions are not implemented. They wait on T29, T4/T18 signature coordination, the T21 Lineglass view, T32's repair-pipeline work, T33's corridor/recharge decisions, and the T35/T36 progression gates.
- **V2 design guards:** interference is agency without violence, never a weapon. Keep its four costs distinct—acquisition, charge, wear, and emission risk—rather than collapsing them into one resource bar. Progression is record-before-fly, then live piloting only after terminal plus receiver access; the exact award route remains unresolved while T36's trust economy is gated. Control is terminal access / misused authorization, not a handheld remote; piloting is T14 viewpoint binding.
- **Art follow-up:** retain the current procedural drone as Draft 1; Dan will provide concept art later for a deliberate visual pass rather than blocking systems work.
- **Owning docs:** `dissonance/THREADS-delta-T31-T32.md` and the executed acquisition scaffold `todo/emitter-drone-prompt-v1.md`. The broader design is canonical here; do not execute the archived delta as a second plan.
- **Ties:** T25 (weather command), T4/T18 (`applyDisruption`), T21 (patrol world + future drone view), T29 (control/review), T14 (future viewpoint binding), T32 (repair pipeline), T33 (corridor/possible recharge), T35 (workshop), T36 (terminal/receiver progression).

### T32 — Repair-pipeline hack
- **Status:** experimental; downstream of T31 terminal control.
- **Scope:** lost drones are repaired and returned to patrol. Milo exploits that queue by presenting a controlled drone as something the system already wants to mend.
- **Discovery:** teach the pipeline through disappearance/reappearance and legible interface state, never explanatory narration.

### T33 — Infrastructure corridor
- **Status:** provisional; gated on the owning prompt’s Phase 0 audit.
- **Scope:** one shared full-axis right-of-way carrying graded rail, high-tension power, an EM charging line, a tunnel, a climbable knowledge-bearing pylon, and PATROL/ALERT binding.
- **Owning docs:** `dissonance/rail-and-lines-corridor-prompt-v3.md` and `dissonance/THREADS-delta-corridor.md`.

### T34 — Acoustic mech disable
- **Status:** experimental; blocked on T9 and T4.
- **Scope:** a learned pattern drives a corridor mech into recoverable DORMANT state via `applyDisruption('acoustic-desync', magnitude)`. Performing it raises player legibility; it never destroys.
- **Open canon:** Resistance resolution versus Synod password, and headphone attenuation versus full negation.

### T35 — Underground network (parent; contains Milo's workshop)
- **Status:** first authored node, validation corridor, and inventory-as-room presentation landed as Draft 1. Live visual review of the hardware display and the already-deferred underground tuning/QA remain; wider network content stays gated.
- **Landed world/narrative chain:** the crater latitude/longitude anchor now holds a weathered fallout-shelter entrance concealed by chain-link fence, dense high-definition world trees, and thicket. Project textures skin the entrance geometry. A consonant locator sounds on approach and cuts permanently at the physical threshold; the doorway leads into a first-person, three-zone workshop. Discovering the workbench sets a durable workshop flag for later construction and underground progression; it does not gate T31 acquisition.
- **Topology contract:** authored nodes carry story, unique inventory, and distinctive silhouettes; seeded procedural corridors connect them without placing story state. The first locked-seed corridor, workshop gate, debug unlock, orientation marks, and production walkthrough landed and were accepted as Draft 1. Detailed acoustic/visibility/traversal validation is deferred to a later underground tuning/QA pass.
- **Worldbuilding guard:** treat the stratum as a pre-Synod, mixed-infrastructure palimpsest presented as off-SignalNet—not a proven sanctuary. Never resolve whether the Synod cannot reach it, tolerates it, abandoned it, or quietly watches it until the narrative makes that ambiguity earn its answer.
- **Player/state foundation:** story flags, recovered hardware, Lineglass inventory, route state, and flashlight ownership now share a versioned World save boundary. The persistent `@dissonance/player` camera flashlight is available underground and gates the corridor test; environment/HUD preferences remain separate. This state integration is a mandatory prerequisite for any later T31 construction or wider underground progression.
- **Inventory-as-room landed (2026-07-31):** persistent empty cradles establish the chassis/emitter silhouettes before acquisition. The stable World-save hardware IDs independently reveal the recovered chassis and emitter, restore on reload, and update immediately after recovery. The exterior source drone hides on recovery and remains hidden after reload or world-feature rebuild, preventing duplicate hardware. Boulevard and workshop presentations share the same app-local ovoid/ring/two-eye silhouette builder; the bench version is inert/scorched, preserving the procedural design Dan liked. This is projection-only: the save remains authoritative, and the display adds no interaction, assembly, mounting, maintenance, terminal, receiver, or piloting behavior. Live visual review remains outstanding because the in-app browser was unavailable; World production build passed.
- **Workshop later slices:** assembly, maintenance, recorded-feed review, terminal/receiver placement, predecessor resolution, and interactable workstations remain gated or deliberately undesigned.
- **Owning docs:** `dissonance/underground-network-concept-v1.md`, `dissonance/story-manifest.md`, and `dissonance/world-save-contract.md`.
- **Ties:** T31 (verb and hardware), T36 (inhabited pole), T6 (inventory-as-room precedent), T9/T20 (enclosed acoustic language), T14 (first-person unwatched depth grammar).

### T36 — Rey Caverns + gatekeepers (inhabited pole)
- **Status:** **Rey Caverns** is the working name. Only the workshop-hallway boundary teaser has landed; inhabited space remains closed.
- **Landed teaser:** a stable-ID, simple humanlike lurker remains visibly framed beyond the hallway gate, then retreats once when Milo approaches or directly illuminates it with the flashlight. `reyLurkerFled` persists. It does not approach, attack, speak, grant inventory, open the gate, define a faction, or establish who/what it is.
- **Mandatory gates:** culture and the trust economy are still in development, so no favors, tiers, residents, gatekeepers, terminal award, or receiver award may be authored yet. Tonal-language rules are parked on the separate Dissonance Audio HUD repository, which has not been adapted into this monorepo. Inhabited space beyond the workshop and validation hallway stays physically gated until those dependencies land.
- **Provisional architectural direction:** Rey Caverns may eventually use real struck stone as a practical signaling/lock system at architectural scale. Keep it material and wary/transactional—not magic, not a triumphant resistance concert—and do not implement pitch rules until the Audio HUD dependency and cultural design exist.
- **Owning doc:** `dissonance/rey-caverns-concept-v1.md`; runtime boundary beat also appears in `dissonance/story-manifest.md`.
- **Ties:** T35 (parent stratum), T31 (future terminal/receiver progression), T9/T20 (future tonal/acoustic language), T10 (culture and Synod scope).

### T37 — Milo's apartment archaeology room
- **Status:** queued; architecture approved, implementation not started.
- **Purpose:** turn the preserved apartment photogrammetry capture into an authored archaeology room entered from the existing World boulevard.
- **Owns:** capture preparation, room-specific preservation treatment, archaeology interaction, and the room's O4 camera pilot.
- **Canonical docs:** `design/locations/MILOS-APARTMENT.md`.
- **Runtime owners:** `apps/world` ⚠VERIFY during Phase 0.
- **State owners:** World save location/archaeology domain, exact shape TBD.
- **Depends on:** T21 world/location substrate, O4, validated Blender export.
- **Consumed by:** Boulevard narrative and Milo's archaeology progression.
- **Decisions:** D45.
- **Open questions:** O4.
- **Implementation:** the existing procedural Milo building/interior and underground workshop are adjacent prior art; neither is the photogrammetry room. The Blender pipeline test, human cleanup, room integration, and live validation remain outstanding.
- **Next gate:** validate the Blender preparation script without creative changes, then perform the separate human cleanup/export pass.

### T11 — Provenance / multiplayer
- **Status:** parked architecture; no network implementation session until T29's offline terminal and in-process Scrambler contract prove the interaction boundary.
- **Scope after v9.43 addendum reconciliation:** server-authoritative sessions, reconnect, terminal messages, player traces/presence, stable-feature replication, event ordering/provenance, and server-owned faction affiliation (Synod / Independents / Chorus / Null). Conflict remains infrastructure-centric and must conform to D2 nonviolence.
- **Transport order:** authoritative WebSockets first; WebRTC is a later media channel, not a second authority path. Shared physics, full-world replication, and synchronization of every visual artifact remain explicitly out of scope for the first network slice.
- **Ties:** T26 (replicated stable identity), T29 (client terminal/Scrambler contract), T14 (viewpoint binding), persistence (event provenance and reconnect state).

### T12 — Mobile control layer
Touch-drag look; gyroscope tilt-to-move (DeviceOrientation/DeviceMotion); standing play as a first-class posture. Calibration moment ("hold comfortably, tap to set neutral") with a cheap, habitual re-center gesture — all tilt measured as deviation from calibrated pose.
- **Architecture:** `ControlProfile` (dead zones, sensitivity curves, inversion, tilt-to-speed mapping, calibration offsets) through a single `applyControlProfile()` seam — same pattern as environment profiles, tunable in Dev HUD, exported to JSON. Gives `@dta/input` its reason to exist.
- **Constraints:** iOS Safari requires explicit permission via user gesture over HTTPS — **one gesture must unlock: AudioContext (D1) + microphone (voice rig) + device orientation + calibration**; gyro yaw drifts. Phone speakers collapse 3D panning — pursuer-critical info must survive in mono channels (volume, timbre, filter, rhythm), direction is a headphone luxury.
- **Status:** design parallel-safe; implementation gated on `@dta/input`
- **Owning doc:** none yet — needs `mobile-control-prompt-v1.md`

### T9 — Sound-as-control → voice rig
The shipped mechanic is the **voice rig / vocoder loop** (see acoustic world systems thread): live signal construction, multi-dimensional authentication, partial-match consequences. Polyphonic synth with keys routes into the same Tone.js graph — the instrument you play is the instrument that hacks; synth carriers feed the vocoder. **The Godot oscilloscope prototype is a museum piece (P8): its single-frequency match-score loop is explicitly superseded by the anti-minigame rule.** Concept reference only.
- **Vocoder as theme made mechanism:** voice as modulator, machine as carrier — human intention passes through technological structure and emerges neither fully human nor fully mechanical. Lola operates *inside this graph* (see lore).
- **Handoff doc now committed:** `sound-as-control-prompt-v1.md` (found in `docs/plans/`, previously unreconciled here) — a scoped Phase-0-gated prototype (`packages/sound-control`, dominant-bin match scoring in a web worker, IDLE/CONFUSED/CONTROLLED drone state machine with hysteresis, canvas oscilloscope) explicitly superseding the Godot `Oscilloscope_prompt` reference doc above. It also carries §5, a **drone-entrainment seam from T13's waveform-locomotion seed** (§2a below): a CONTROLLED drone's `EmbodimentProfile` locomotion params lerp toward the player's played frequency/amplitude, so the tell of control is the drone's body moving to your waveform, not just a meter.
- **Gate is narrower than "T3 lands":** the prompt's own Phase 0 audit asks it to confirm `@dissonance/audio` exposes named buses + attachable analyser taps. Per the 2026-07-17 T3 audit, Phases A/B are done but Phase C (`PursuerAudio` → `@dissonance/audio`) is deliberately deferred — so the bus/analyser surface this prompt assumes may not fully exist yet even though "T3" in the loose sense has landed. Whoever opens this thread should re-audit `@dissonance/audio`'s actual exports first, not assume the gate cleared when O10/O11 did.
- `packages/sound-control` does not exist yet — confirmed, no implementation session has run.
- **Status:** horizon; depends on D1 bus scaffold + acoustic domain types (T20 Phase 1) + the narrower audio-surface gate above

### T13 — Godot PoC excavation (reference only) 🔄 UPDATED
Surveillance Boulevard PoC mined for: boulevard axial layout (urban-edge level design); two-state patrol design (waypoint rounds → detection-triggered pursuit — ambient, not hunting; maps to a `BehaviorProfile`, confirming drone and DTA pursuer share one skeleton); break-and-hack seeds (→ `applyDisruption`, future mechanics thread).
- **Analysis pass landed:** `t13-boulevard-extraction-v1.md` (found committed in `docs/plans/`, previously unreconciled here) — the stills-capture next-action from the prior entry happened; this doc is the resulting extraction. Reference only, no code ports, per its own header rule.
- **Layout notes (→ T6.2 urban-edge profile):** axial boulevard with tower verticals is the level-design spine — legibility of the watcher's territory *is* the level design; globe-in-cage street lamps carry forward as a distinctive urban-edge prop (rhymes with the catenary-wire vertical rhythm); emissive amber windows against teal/black confirm the T6.2 window-occupancy read (sparse warm rectangles signal curfewed habitation with zero interior geometry); wireframe-over-mass aesthetic ("the world rendered the way SignalNet parses it") is the PoC's strongest visual identity, provisional canon-adjacent, worth preserving as a T6.2 post-process direction.
- **Drone patrol notes (→ T4 `BehaviorProfile`):** fixed waypoint loop, two-state brain (patrol/pursue), ambient not hunting. **Route-visualization toggle is the single highest-value dev-tool extraction** — generalizes into T1/T2 as an any-agent waypoint/path overlay (already cross-referenced there).
- **New seed — waveform locomotion (§2a, provisional, AGENTS.md candidate):** the drone's flight path *is* a sanctioned waveform — perpendicular offset from path tangent, distance-parameterized (not time-parameterized, so speed changes don't distort the wave shape), axis-swapped by tangent frame. `BehaviorProfile` modulates it per state (patrol: clean fixed-frequency sine; pursue: frequency drifts up, amplitude tightens; disrupted: phase jitter — a corrupted drone visibly *moves wrong* before anything else happens). **Directly feeds T9**: a CONTROLLED drone's waveform entrains toward the player's played signal, fusing the oscilloscope UI with the drone's visible body — see T9's new §5 note. Constants live in `EmbodimentProfile`; fold into `pursuer-embodiment-prompt-v1.md` when T4 unblocks (outstanding item 3 below).
- **Break-and-hack seeds (→ T4 `applyDisruption`):** fault-relay "bring component, socket into device" loop inverts cleanly into sabotage — one interaction grammar, two moral directions, zero new UI. The PoC's music-note pickup iconography for signal components should **not** be carried forward as-is (too literal for Dissonance's tone) — route the collect-and-socket *loop*, not the icon.
- **New seed — interior-state/wellbeing HUD (unrouted, needs a thread decision):** the PoC tracks Milo's own psych state (Signal/Coherence/Focus/Energy/Mood/Social Load/Sensory Load/Burnout Risk) as a resource system, zone-scoped (boulevard = neutral, domicile = recovery) — the `EnvironmentProfile` pattern applied to a psychological layer. Thematically strong ("the Synod doesn't need to punish you, only deplete you") but a large mechanical surface with real pacing risk (sim-management drift away from atmosphere/restraint). **Disposition: logged as experimental, unrouted — do not fold into T9 or T5 implicitly.** Needs its own design pass and a Dan decision on whether to open a thread for it at all.
- **Canon-risk register (flag provisional, do not resolve unilaterally):** waveform locomotion implies SignalNet agents are literally signal-shaped in physical space; wireframe-as-SignalNet-render overlaps the T10 isometric-camera-rule claim about "how SignalNet sees" — the two must not contradict, resolve alongside O4's sign-off.
- **Outstanding:** (1) video capture of the patrol loop in motion (detection trigger, pursue behavior, return-to-route) — the extraction doc is explicit that this remains uncaptured, drone was docked at route root in every still; (2) Dan sign-off on the wellbeing-HUD disposition; (3) fold §2a's waveform constants into `pursuer-embodiment-prompt-v1.md` once T4 unblocks.
- **Status:** partial — stills + written analysis landed; video pass of the patrol loop is the one remaining capture task before this doc is complete

### T10 — Lore / AGENTS.md (parallel)
- **T-Lola:** counterpart persona, ontology branch-dependent (data-ghost / expressed self / anomaly-adjacent), never fully confirmed; functions as auditory disguise degrading SignalNet classification. Isometric rendering = diegetic SignalNet perception; Lola-active state degrades player legibility in that view. *Status: experimental.*
- **T-Lola addendum:** fourth manifestation — hallucinatory nightmare figure; a failure mode of the other three, not a fourth ontology. Same rig/silhouette degraded via animation corruption or shader treatment. Candidate mechanism: Synod acoustic experimentation damage — identity-layer expression of the same phenomenon as DTA creatures. Open: trigger conditions; whether player-as-Lola sessions can be visited by nightmare-Lola. *Status: experimental.*
- **T14 — DTA pursuer ↔ nightmare-Lola link** via viewpoint binding. *Status: provisional.* **Note: viewpoint binding is now narratively load-bearing** — the DTA spine's perspective shifts and ghost-of-you reveal depend on it (implementation still aspirational; design artifact `data-as-identity.svg`: identity is the resolved profile; possession/hacking = re-binding the viewpoint; skinchanger and hacker are one mechanic wearing two fictions).
- **T14 — first planned Dissonance consumer:** T31's eventual receiver-enabled, terminal-addressed drone piloting. This remains design-only; acquisition v1 does not bind the viewpoint.
- **Pursuer gender-ambiguity design** (silhouette, pitch-ambiguous vocalization, gait corruption). *Status: provisional.*
- **Spiral placement rule (T15/T16, provisional):** collectibles placed on an outward spiral from the parking-lot hub — distance rings made continuous, with deterministic angular progression. Authoring/testing win: placement is a function (radius, angle), not a hand-scatter; walk order is enumerable for playtests. Spiral points **snap to nearest reachable trail-network position** (pure geometry would strand items off-trail). Known tell: pattern-literate players may deduce the spiral — acceptable for now, revisit with jitter/per-ring angular offset later; the trail network already distorts the spiral enough that on-foot legibility is low. Pursuer escalation stays keyed to *radius*, so the spiral changes nothing about T15's difficulty bargain.
- **T15 — DTA narrative spine** and **T16 — single-level hub structure** (per `dta-session-notes.md` §5–11; §1–4 indexed under "DTA forest visual & play-scenario direction" above). *Status: provisional/experimental as marked.*
- **Isometric camera rule (experimental, needs O4 sign-off):** monitored interiors snap to isometric (SignalNet's legible read); private spaces stay first-person — camera grammar diegetically signals observation. Domicile stays first-person until a one-shot scripted beat renders it isometric (home is watched; zero dialogue). Extensions: (a) player body visible only in surveilled view — the system sees you as a legible object; found clothing reads as data; (b) **inventory-as-room** — collectibles physically displayed in the domicile, a shelf is an inventory screen (ties to collect-and-leave vision + provenance); (c) **staged confrontation framing** — fixed side-on isometric shot for scripted encounters; theatrical legibility without combat (D2 holds; tension is who moves/speaks/backs down). Control during shift: same movement input, camera reframed only.
- **AGENTS.md candidates:** mutual legibility, ghost-of-you reveal, sleep-hike ambiguity rule. *Flag provisional.*
- **Signal coverage as geography (provisional, Dan 2026-07-15) — one gradient, two fictions (T14 pattern):** cell/network coverage as a spatial storytelling layer in *both* games, with inverted valence. **DTA:** signal = safety — bars mean orientation and the world still existing; coverage decays along the T16 distance rings; the tier-1 phone recovery beat lands on **NO SERVICE** — the phone works, it just can’t save you; recovering it confirms depth instead of granting rescue. **Dissonance:** signal = surveillance — SignalNet coverage is oppression; dead zones are where the Synod can’t hear: where music might be safe, where the rusted emitter arrays and wrong-behaving animals are (creature-damage proposal); freedom and danger share the gradient. **Engineering:** coverage is one more channel in the T23 region mask — same texture-lookup machinery as concealment; zero new systems. **Open design question (do not resolve unilaterally):** the recovered phone’s screen glow vs. P5/T15 no-light-source rule — candidates: dead battery, cracked-dim screen, or a light/noise/battery tradeoff. **Canon-guard note:** whether the two games’ coverage maps are literally the same map stays unhardened with the shared-universe question.
- **Underground knowledge guard (T35):** presenting the workshop/network as off-SignalNet does not establish that it is unknowable or safe. Keep open whether the Synod cannot reach it, tolerates it, abandoned it, or quietly observes it; no later node may answer that incidentally through generic flavor text.
- **Acoustic-world canon candidates (per `acoustic-world-systems-prompt-v1.md`, flag provisional):**
  - **Disarmament contradiction:** the Synod confiscated civilian weapons "to end private violence," then made coercion environmental — language authorizes force, sound enacts it. Guns are rare narrative objects, never player mechanics (D2 holds); a single gunshot is rare and significant.
  - **Ambiguity of compliance (core theme):** a person who kneels cannot determine whether it was choice, fear, habit, imitation, or body-before-mind. Conditioned public choreography (stop / disperse / kneel / face forward / clear streets) emerges from conditioning + fear + physiology + social imitation — never magical mind control.
  - **Sensory punishment:** the institution treats bodily faculties as revocable privileges — euphemisms: *sensory revocation, auditory forfeiture, visual suspension, manual disqualification, civic incapacitation*. Expressed through adaptation, testimony, prosthetics, sign languages, tactile warning systems, protected acoustic spaces — never gore or spectacle. **Affected characters remain complete people with agency and expertise, not storytelling props.**
  - **Creature damage canon strengthened:** the pending acoustic-experimentation proposal now sits on systemic ground — cumulative dose, entrainment, ecological breakdown are simulated phenomena, not backstory. The Synod is not omniscient; sound is not magic. **Continuous-world canon guard (T23):** the one-world build is a *shared engineering world* — one playable space for construction and testing — and does **not** commit the fiction to shared DTA/Dissonance geography. Cemetery stays the deliberate seam; the shared-universe question remains open until the T10 narrative pass resolves Synod geographic scope deliberately.
  - **Lola, mechanical role:** operates inside the voice graph — supplies harmonics, corrects timing, splits voice into layers, derives carriers, suppresses stress markers. Ontology guard extended: **do not assume data-ghost is primary.** The mechanic itself performs the ambiguity — is she filtering your voice, replacing part of it, or speaking through you? A successful disguise may leave the player unsure the accepted voice was theirs. *Status: experimental; do not make ontology explicit early.*

## Decisions (frozen — do not relitigate in sessions)

**D1. Audio engine: Tone.js owns the AudioContext. Babylon never plays sound.** One context, one master chain in `@dta/audio`. Named buses: spatial / ambient-beds / interior (heartbeat, stings) / music-synth. Ducking constants defined once, between buses. Spatialization via `Tone.Panner3D` + `Tone.Listener`. Babylon's sole audio role: a bridge system syncing listener/emitter positions from the render loop (~15–20Hz throttle). AudioContext unlock shares T12's permission+calibration moment on mobile. Panner budget: dozens OK, hundreds not — swarm life stays in 2D beds. Keep `Tone.Transport` (musical time) distinct from game-loop time.

**D1a. Thread split.** Tone.js on the main thread; native WebAudio DSP on the browser audio thread; custom synthesis in AudioWorklets; FFT/match-scoring in a web worker.

**D2. Nonviolence.** The player is never given the option to be violent. No melee, no damage, no HP, no forced stagger. Objects affect the world only through sound. "You are prey" is a design principle, not a mechanic choice. Supersedes the stagger/repel stick design in full.

---

## Principles

- **P1. Extend, don't rewrite** — `EnvironmentProfile`/`applyProfile()`, `getHeightAt`, thin-instancing are stable; new systems conform.
- **P2. Audit before implement** — every handoff prompt carries a Phase 0 audit gate.
- **P3. Named constants, never hardcoded values.**
- **P4. Gates between phases** — Dan signs off; decisions are not resolved unilaterally.
- **P5. Warm light belongs to trail markers** — fireflies yellow-green only, never orange/amber; navigation legibility is a hard rule.
- **P6. Diegetic coherence** — isometric camera as SignalNet perception, Lola as auditory disguise, detection state driving environment reactivity.
- **P7. Handoff prompt as primary artifact** — web sessions produce versioned, self-contained prompt docs; prior versions explicitly superseded.
- **P8. Playable archive** — the project retraces its own path; superseded implementations (Godot Surveillance Boulevard, the Godot oscilloscope prototype — see T9, the current `dont-turn-around` app) become museum pieces once their concept migrates onto its successor foundation. *(Corrected 2026-07-14, Dan, local trail-viewer session: trail-viewer is the **successor foundation**, not a museum piece — the DTA concept moves onto trail-viewer’s terrain/water/atmosphere/traversal systems, and the present `dont-turn-around` implementation is preserved as the frozen reference instead. This aligns P8 with T23’s DTA-onto-TrailViewer plan, which the prior wording contradicted.)*
- **P9. Baseline world first** — build the ordinary, healthy, inhabited world (ecology, settlements, ordinary voices, ordinary silence) before applying Synod transformations. Wrongness only reads against an established normal. The Synod changes a world; it is not the world's identity.
- **P10. Audible ≠ harmful** — audibility and biological exposure are separate values everywhere. A faint infrasound signal can harm; a loud waterfall can mask without harming. No system may conflate them.
- **P11. Transform reality, not assets** — prefer profile-driven transforms, perception, acoustics, and continuous spatial fields over bespoke duplicate geometry. This does not override P9: establish the ordinary baseline before applying Synod transformation, and keep canonical simulation coordinates distinct from perceptual distortion until O12 is resolved.

---

## Open decisions (pending Dan's sign-off)

1. **O1. Vertical exaggeration factor** (geo pipeline) — couples to `walkableSlopeAngle` in the navmesh bake.
2. **O2. Tile-based navmesh bake — DECIDED: required** (T23 continuous world + pursuer route cutoffs at reservation scale). Remaining question is bake granularity/tooling only.
3. **O3. Terrain stamps** — ride on the tile-bake decision, or separate?
4. **O4. Isometric camera rule** — experimental; no level design may assume it before sign-off.
5. **O5. First-encounter jump-scare flag persistence** — persisted across sessions (decided in web session, restated for sign-off): first real consumer of `@dta/persistence`.
6. **O6. Bus naming collision — RESOLVED (Dan, 2026-07-14):** D1 names stand (`spatial / ambient-beds / interior / music-synth`); `acoustic-world-systems-prompt-v1.md` conforms to D1 before use. Reopen only with a compelling reason per standing D-series rule — closed, not welded shut.
7. **O7. `SensorProfile` as fourth profile axis** — schema change to the resolved pursuer/mech-dog profile; `pursuer-embodiment-prompt-v1.md` needs a v2 incorporating it before that session runs.
8. **O8. Acoustic-world sequencing vs. audio single-writer** — Phase 1 (pure types) parallel to T3; Phases 2+ queue behind extraction. *Decided in the web session and already stated as settled in T20’s text — restated here for formal sign-off (same pattern as O5), so the doc stops contradicting its own P4.* **Reopened by the 2026-07-17 T3 audit (not resolved unilaterally):** T3's audio-touching phase (C, PursuerAudio → `@dissonance/audio`) was deliberately deferred, not landed — if T3 isn't going to touch `@dissonance/audio` on its current trajectory, this queueing logic may no longer apply as written. See O11.
9. **O9. Transition compose rule (T25/T5/T23)** — proposed: region selects profile family; clock interpolates keyed variants (base); detection grading overlays with named blend weights. One lerp system, three drivers. Confirm or amend before the T1 schema session.
10. **O10. Does T4 need T3 Phase C?** (added 2026-07-17) — T3's Phases A/B (PursuerSystem, HeartbeatGlow split) are already done; only Phase C (PursuerAudio extraction) was deliberately deferred pending a second consumer. T4 wants an `AudioProfile` composable axis — does that requirement actually need `PursuerAudio` to already be a package, or can T4 start now? Unblocks T4 immediately if the answer is "A/B is enough."
11. **O11. Does O8's audio single-writer queue still apply?** (added 2026-07-17) — O8 queues T20 Phases 2+ behind T3's audio-touching work. T3's audio phase (C) isn't happening on its current trajectory (see T3 entry). Either re-scope O8 to whatever eventually does Phase C, or confirm T20 Phases 2+ can proceed now that nothing is actively about to write `@dissonance/audio` under T3's name.
12. **O12. Perceptual vs. authoritative spatial distortion** (added 2026-07-27 from the v9.20 addendum reconciliation) — proposed first slice keeps WGS84/world simulation, navigation, collision, saves, and multiplayer canonical while zone fields distort rendering/atmosphere/audio only. Confirm before Synod Capital work whether any later design actually requires authoritative geometry deformation; that choice would expand replication, physics, and navmesh scope substantially.
13. **O13. Rust/cracked-plastic/duct-tape swatch crops** (T30, added 2026-07-29) — need dedicated higher-res reference crops; only ~65×65px thumbnails exist today. Skipped rather than baked from an 8x upscale.
14. **O14. Atlas vs. single-tile for scatter variation** (T30, added 2026-07-29) — moot until a second swatch family exists; the handoff doc's open decision #2 restated, not resolved.
15. **O15. Echo-17 reference-art licensing** (T30, added 2026-07-29) — AI-generated project-original art, supplied directly by Dan; generating tool/account's output-rights terms not independently verified this session. Each baked texture's `ASSET-LICENSE.txt` flags this rather than asserting it's cleared — confirm before any of it ships in a build others see.
16. **O16. Seamless-tiling quality bar** (T30, added 2026-07-29) — the offset-and-heal technique leaves a faint seam on close inspection; acceptable for a first pass. Decide whether that's good enough long-term or whether a real content-aware seamless-texture tool is worth adopting before more swatch families get baked.
17. **O17. T31 terminal authorization model** — misuse of Milo's legitimate authorization vs. a found credential. Terminal-not-handheld is fixed; decide before T29/T31 v2.
18. **O18. T31 recharge source** — utility-corridor proximity vs. passive recharge vs. salvage. Decide before charge implementation.
19. **O19. T31 receiver persistence** — permanent unlock vs. losable/revocable hardware. Decide before any T36 award path is authored; that path itself remains gated on culture and trust-economy design.

---

## Execution plan

### Current underground/interference lane — 2026-07-31

```text
T25 precipitation/storm foundation ✓
  → T31 acquisition v1 ✓ (workshop-independent; stronger-machine live QA follow-up deferred)
T35 shelter/workshop/save/corridor Draft 1 ✓
  → T35 inventory-as-room presentation ✓ (live visual review pending)
  → deferred T31/T35 tuning validation LATER (per Dan)
  → T31 v2 interference/control GATED
  → T36 inhabited Rey Caverns GATED
```

The authorized presentation lane now reaches its gate. Live visual review and the T31/T35 tuning pass remain explicitly deferred until later. Do not cross into assembly, maintenance, terminal/receiver control, `applyDisruption`, or inhabited-space authoring. Further work requires a deliberate prerequisite decision for T29/T31 v2; do not infer that decision from this implementation.

### Legacy cross-project critical path (retained for audit; not the current lane)
```
T3 pursuer extraction → T4 embodiment → T23 region system → T5 reactive seam
```
T3 was believed to be the single biggest unlock still to run. **2026-07-17 audit found most of it already landed** (Phases A/B — PursuerSystem, HeartbeatGlow split — both done, not recently; only Phase C, PursuerAudio extraction, remains, and it's deliberately deferred, see T3's entry and O10/O11). The "run the prompt, unlock everything downstream" framing below is stale — read T3's own entry before assuming anything here is still blocked on it.

### 🟢 Ready now (parallel-safe, no blockers)
| Thread | Next action | Who/where |
|---|---|---|
| **T3** | Nothing to run — Phases A/B already landed. Remaining: sign-off on O10 (does T4 need Phase C?) and O11 (does O8's audio queue still apply?). No local session needed until those resolve. | Dan (sign-off only) |
| **T1/T2** | Continue HUD work; add agent-route visualization toggle (from T13) | local session |
| **T17** | Attempt Tier 1 (three-cube arm round trip) | Dan, Blender |
| **T10** | Lore/AGENTS.md passes; Synod geographic scope question | web session |
| **T20 Phase 1** | Typed acoustic domain + tests — **prerequisite: find-replace bus names to D1 in `acoustic-world-systems-prompt-v1.md`** (O6) | local session (types only, no `@dta/audio` writes) |
| **T21** | Continue trail-viewer dev — **default serial with T24** (same app; see T24's concurrency advisory) | local session |
| **T24** | Run `forest-performance-prompt-v1.md` (Phase 0 audit first — gate) — **default serial with T21**; if concurrent, T24 stays fenced to forest modules only | local session, app-local |
| **T12 design** | Draft `mobile-control-prompt-v1.md` (design only) | web session |

### 🟡 Ready with caveats
| Thread | Caveat |
|---|---|
| **T18 throw & distraction** | Kinematic-arc half is parallel-safe; the impact→noise-event half touches `@dissonance/audio` → that portion queues behind whatever ends up doing T3 Phase C (see O11 — the queue's premise changed 2026-07-17). Either split the session or wait for O11 sign-off. |
| **T19 wildlife** | Same split: fauna state machines parallel-safe; audio-primary birds/flush events queue behind T3 Phase C / O11. |
| **T13** | Stills-capture + analysis pass done (`t13-boulevard-extraction-v1.md`, 2026-07-22 — see T13's updated entry). Remaining: blocked on **Dan**, not code — a video pass of the patrol loop in motion (detection trigger, pursue behavior, return-to-route). |

### 🔴 Blocked
| Thread | Blocked by |
|---|---|
| **T4** | **T3's blocking status is now unclear — see O10** (Phases A/B it may actually need are done; only Phase C, deferred, is outstanding) + mech-dog `.glb` (T17 Tier 2) + **O7** (embodiment prompt needs v2 for SensorProfile before running) |
| **T23** | T3/T4; interior regions additionally gated on the interiors schema pass |
| **T5** | T3 + stable profiles; build jointly with T23's transition seam (one system) |
| **T9 impl.** | T3 (D1 scaffold) + T20 Phase 1 types |
| **T20 Phases 2+** | T3 (`@dissonance/audio` single-writer) — **see O11, this may no longer be a real blocker** |
| **T6/T7** | T23 + interiors schema pass |
| **T11/T14 impl.** | Aspirational — no sessions |

### Decision gates on the critical path (Dan-only, cheap to clear)
- **O10, O11** (added 2026-07-17) — cheapest gates on the board right now: one word each potentially unblocks T4 and T20 Phases 2+ immediately, since the code-side work they gate (T3 Phases A/B) already happened
- **O7** — SensorProfile as 4th axis → unblocks writing embodiment prompt v2 *now*, so T4 starts the day T3 lands
- **O1** — vertical exaggeration → couples to `walkableSlopeAngle` → navmesh bake (O2-decided) → T23
- **O3** — terrain stamps ride tile-bake or separate → T23 interiors
- **O5, O8** — already decided in-session; formal sign-off = one word each
- **O4** — isometric camera rule; blocks nothing yet, but no level design may assume it

### Historical suggested sequence — superseded

The former v9.7 “this week” list is removed because its T3 assumptions were later disproved by audit. It is not an executable queue. Use the current underground/interference lane above; regenerate the broader cross-project plan before resuming it.

---

## Parking lot

- Screenshot capture as state snapshot — serialize camera position/target, exaggeration, palette, generation params to JSON; PNG export as later layer. *(JSON half implemented 2026-07-14/15 as the Copy/Load View mechanism under T21 above — PNG export still pending.)*
- Shadow-as-exposure pursuer mechanic — revisit later.
- **[T11/T14]** Provenance/multiplayer + viewpoint binding — implementation aspirational, explicitly out of scope for current pass (but see lore note: now narratively load-bearing).
- Ambient dread roster: vultures; false-scare roster (vulture burst / live-dog-vs-mech / understory crash) with cry-wolf budget of 2–3 per playthrough, heartbeat+vignette must participate in fakes; first-attack jump scare with persisted one-shot flag, telegraph stagger clip, first-time + toned-down sting variants, sting-ducks-heartbeat named constant.
- Creature behavioral damage canon — pending proposal linking to Synod acoustic experimentation (now also referenced by T-Lola addendum).
- **[T7]** World population; Dissonant Boulevard window occupancy figures (rare silhouettes crossing lit windows — UV-scrolled mask or quads behind glass, ~3 sightings per walk, never reacting; a silhouette that stops and turns is a one-shot scripted beat). Interior sound leaks from lit windows (2D bed, proximity-gated).
- **[T7] "Door to nowhere" — found, not built (Dan, 2026-07-27 screenshot, "dissonance boulevard"):** one of the city-kit building instances has a door positioned on its facade with no balcony, landing, or stairs reaching it — a Quaternius modular-kit artifact (not authored by us), flagged by Dan as a diegetic narrative opportunity rather than a bug to fix. **Deliberately left alone** — per the diegetic-discovery filter already governing the dead fountain/giant snag/shed props (T22), an unexplained impossible door earns its own read better than an explanation would. Recorded here so it doesn't get silently "fixed" in a later building-geometry pass; revisit if/when T7's window-occupancy or a hero hardscape variant of this building type gets built for real.
- **[T5/T6]** Interior schema pass + reactive environment seam (after embodiment work). Interiors to bracket: liminal mall (long repeating corridors) vs. mausoleum (tight stone) vs. Milo's domicile (`RoomProfile`: acoustics, light sources, anchor props + scatter kits; issued furniture, ration packaging, no sound-making objects; candidate anomaly seed). **Interiors gain a mechanical job (acoustic-world prompt): rooms as diegetic audio processors** — mausoleum reverb, water-tank resonance as `EnvironmentalAudioProcessor` stations in the voice-rig chain.
- **Acoustic persistence roster (from acoustic-world prompt):** cumulative exposure, long-term ecological changes, broken emitters, hacked devices, discovered carrier signals, voice-rig presets, animal route changes, known command patterns, permanent character effects. Versioned schemas; never persist transient DSP params.
- Hold-to-aim throw strength (noted as future knob in throw slice).
- **[T6]** Landscape targets: forest, urban-edge, liminal mall, cemetery, mausoleum, parking lot (dead-commerce exterior; also the DTA hub lot), Milo's domicile. Cemetery = open sightlines + dense low occluders (vision-dominant play); warehouse complex = interior occlusion (hearing-dominant play, surface-typed footsteps).
- **[T21]** From `docs/NOTES.md` (Dan, committed 2026-07-17): (1) **bug, still open** — vertical elevation renders completely absent/broken when viewing trail-viewer on a Mac (platform-specific, cause unknown, not investigated yet); (2) **feature request, DONE (2026-07-22)** — toggleable lat/long overlay on the map, for hand-placing landmark-manifest entries; shipped as `packages/geo`'s graticule module + a trail-viewer toggle, see T21's dedicated bullet above.
- **[T7/T23]** NOTES.md's original framing for item 2 above expected the lat/long overlay to feed straight into `landmarks.geojson` hand-placement. That specific manifest file still doesn't exist — see the new T7-substrate note under T23 (two competing unbuilt placement-manifest designs, not reconciled with the runtime-scatter approach that actually shipped).
- **Godot Boulevard PoC video capture** — the still-frame analysis pass (T13) is done; the patrol-loop-in-motion capture (detection trigger, pursue behavior, return-to-route) is the one remaining piece before T13's extraction doc is complete.
- **Interior-state / wellbeing HUD** (from T13's PoC excavation, new 2026-07-22) — Milo's own psych-state resource system (Signal/Coherence/Focus/Energy/Mood/Social Load/Sensory Load/Burnout Risk), zone-scoped self-regulation under surveillance. Logged experimental/unrouted per T13's own disposition — needs a dedicated design pass and a Dan decision on whether it becomes a thread at all before anything folds it into T9 or T5.
- **[T19/T22] Forest fire mechanic parked, two new narrative/mechanic ideas attached (Dan, 2026-07-24):** trail-viewer's `ForestFire` (`packages/world/src/ForestFire.ts`) is a real, working POC — ignite the nearest tree (F key), fire spreads to unburnt neighbors within a configurable radius over time (throttled by a max-active-fires cap), burning trees swap to a blackened stand-in silhouette after a burn duration; doesn't touch the original `ThinInstanceTrees` buffers. Its dev-HUD exposure (Ignite/Reset buttons, the "fires burning" line in the on-screen readout) was removed 2026-07-24 to declutter — **the system itself is untouched and still fully wired** (F-key binding intact), just not advertised in UI right now. Recorded here so it isn't lost: Dan has (1) an idea to use forest fire as a mechanic/atmosphere element in Dissonance itself (not just a trail-viewer tech demo), and (2) an idea for a firefighting minigame, similar in spirit to a DTA (Don't Turn Around) side-mechanic. Neither idea is designed yet — idea stage only, no scope/threading decision made.
- **[T4/T5/T9/T21] Scale distortion as a sound-as-weapon hallucination channel (Dan, 2026-07-23, idea stage — not built):** trail-viewer's own H/V-scale multiplier systems (`scaleTuning.hScale`/`vExag`, plus the per-cluster `treeScale`/`trailsideScale`/`bulkForestScale` from the same session) suggest a narrative reuse — when the Synod (or an equivalent sound-as-weapon actor) hits the player, a *hallucination* state could distort the player's own perceived world scale rather than (or alongside) any diegetic environment change: trees/terrain reading as subtly or grossly wrong-sized, warping back once the effect clears. Diegetic and audio-first-compatible in spirit (a *perceptual* effect, not a real one — nothing else in the world actually changes size) — reads as a natural extension of T9's sound-as-control spine and T4's Synod/pursuer embodiment work, and would want to share transition/lerp machinery with T5's reactive-environment seam (T23 already notes T5 and T23 share that machinery). Needs a real design pass before scoping: is this per-axis (H vs V independently) or uniform; does it affect the whole visible world or a radius around the player; does it ramp with exposure/proximity like the existing adrenaline systems, or trigger as a discrete hit-reaction. Not prototyped.

---

## Conflict rules

- Contradictions between design docs and codebase are surfaced and analyzed, never silently resolved.
- Narrative additions enter at provisional/experimental and are elevated deliberately.
- Frozen decisions (D-series) are recorded here and not revisited without an explicit Dan-initiated reopening.
- **This file is single-writer per session** — two v7s existed because two sessions consolidated in parallel. The repo copy is canonical; web sessions propose diffs against it, never a parallel full version.

---

## Revision log

| Version | Date | Change |
|---|---|---|
| 9.55 | 2026-07-31 | Reconciled the legacy `docs/plans/` inventory against `DISSONANCE-DOCSYSTEM.md`: established the missing SYSTEMS/DECISIONS/OPEN-QUESTIONS registries, classified every plan in `engineering/PLAN-RECONCILIATION.md`, created scoped canonical design homes, and registered T37 for Milo's apartment without renumbering existing threads. Physical plan moves are deliberately a later mechanical pass; legacy paths remain readable but are non-canonical. |
| 9.54 | 2026-07-31 | T35 inventory-as-room presentation landed: persistent empty workshop cradles and independently save-driven recovered chassis/emitter displays update immediately on recovery and restore on reload. The exterior source hides durably to prevent duplication, and both locations share the established procedural drone silhouette. No construction/control mechanics were added. World build passed; live visual review and the broader T31/T35 tuning pass remain explicitly deferred. The lane now stops at the T29/T31 v2 and T36 gates. |
| 9.53 | 2026-07-31 | Folded the underground/interference delta into the stable registry without overwriting IDs assigned by v9.51: incoming T31 detail merged into canonical T31, the underground network/workshop became T35, and Rey Caverns became T36; canonical T32 repair-pipeline, T33 corridor, and T34 acoustic-disable threads remain intact. T25 precipitation/storm, T31 acquisition, T35's shelter/workshop/save/corridor foundation, and T36's boundary lurker are landed. Inventory-as-room is next. T31 v2, inhabited Rey space, trust progression, and tonal language remain gated; stronger-machine live QA and detailed underground tuning remain explicitly deferred. The superseded workshop prerequisite was not restored. |
| 9.52 | 2026-07-30 | Completed T31 v1 acquisition: authored/validated strike profile, Dev Lineglass tuning and JSON round-trip, one canonical drone-settle value, durable anchor/downed-position continuity, and executable deterministic gate/recovery tests. Removed the undocumented workshop prerequisite so the beat again depends only on weather, proximity, and witnessed LOS as its owning brief specifies. |
| 9.51 | 2026-07-30 | Reconciled staged design deltas with the stable registry: emitter/captured-drone and repair-pipeline work became T31/T32; corridor and acoustic-disable work became T33/T34. Removed duplicate `plans/` copies in favor of canonical `docs/dissonance/` artifacts. |
| 9.50 | 2026-07-30 | Milo's building interior made functional: hinged front + apartment doors (proximity swing, real collision blocker), a skinnable lobby carved into the ground floor with wall-chain colliders, stair railings, and a decimated real couch (`@gltf-transform/cli`, 285k→35k tris). Fixed duplicate `"milos-building"` tags across the two boulevard blocks and added a dedicated `locations.json` nav pin. |
| 9.49 | 2026-07-29 | **T30 opened and landed (first pass):** `instanced-material-pipeline-prompt-v1.md` run locally — new `packages/materials` (`ScatterVariationMaterialPlugin`, `createScatterMaterial`, `EmissiveDataPatternMaterial`), baked textures under `apps/world/public/textures/echo17-*/`, verified live via a new dev-only `apps/materials-demo` (144 thin instances, both emissive planes, no shader errors). Package-location open decision resolved to `packages/materials` (Dan) — DTA confirmed permanently out of scope for new work, not just this thread. Four items deferred, not resolved: three of five named swatch families skipped (source-resolution too low), atlas-vs-single-tile moot until a second family exists, offset-and-heal tiling leaves a faint seam (mirror-quad tried first, rejected — kaleidoscope artifact), and Echo-17 reference-art licensing not independently verified (Dan-supplied AI-generated project art, flagged in each texture's `ASSET-LICENSE.txt` rather than assumed clear). New O13–O16; constants/tradeoffs detail in `instanced-material-pipeline-constants.md`. |
| 9.48 | 2026-07-28 | Docs folder reorg (Dan: "can you reorganize the docs folder for me"): extended the `dissonance/`/`dta/` per-game split Dan had already started to the rest of `docs/` — `trail-viewer-poc/`→`dissonance/world/`, `game-story-and-trails-plan.md`→`dissonance/`, `monorepo-docs/`→`monorepo/`, `generation-systems-audit.md`→`monorepo/`, and a new `archive/` for the two superseded THREADS docs. Moves only (`git mv`, no content rewrites); every broken cross-reference across the repo (2 app READMEs, one source comment, one plans/ doc, and this file's own Doc Inventory + 3 inline citations) found via a full-repo grep and fixed; `docs/README.md` rewritten to match. `plans/` deliberately left flat. Full detail in the Doc Inventory section's new reorg note. |
| 9.47 | 2026-07-28 | Persistence/export parity audit (Dan: "every editable param to be saveable to storage and to exported jsons"): both Copy-View `buildSnapshot()`s gained the 3 fields `persistSettings()` already had (`hudVisible`, `worldBounded`, `lineglassPartIds`). Bigger gap found and fixed: the Toggles-section visibility checkboxes (terrain/OSM/GPX/water/clouds/grid/mountains/power lines/mech dog) were deliberately session-only until now — `createVisibilitySignals()` takes a defaults object, `SavedSettings` gained 9 `*Visible` fields, all three serialization sites updated. Grid/OSM/GPX carefully composed with existing Lineglass unlock gating rather than bypassing it. Full detail in T21's matching bullet. |
| 9.46 | 2026-07-28 | Lineglass parts: a small, explicitly scoped slice of `dissonance-lineglass-engineering-review-prompt.md` (§7.6 geo-reference layer + §6 progressive unlock only, not the full 21-section device) — 3 diegetic pickups on the boulevard, proximity-collected, progressively unlock the previously-always-on grid/GPX/OSM HUD toggles (1 part→grid, 2→GPX, 3→OSM). New `state/lineglass.ts` + `world/LineglassParts.ts`; `LocationEntry` gained `lineglassParts`; collected ids persist through the existing autosave path. Full detail in T21's matching bullet. |
| 9.45 | 2026-07-27 | Underwater look: player-mode game loop compares the active controller's Y against the water plane's current render-space level and overrides scene fog to a denser, murkier color/density while submerged, restoring the atmosphere-driven fog the instant the player surfaces (edge-triggered, doesn't fight the Fog slider's own live effect() while above water). Fog-only first pass — no screen tint/post-process. Full detail in T21's matching bullet. |
| 9.44 | 2026-07-27 | Boulevard obstacle collision extended from buildings-only to bollards/planters/street-lamps too (`OBSTACLE_COLLISION_RADII`, radii from each glTF's own real bounds); boulevard doubled a second time (52→104 placements, 18→36 buildings), same seamless-offset method as the first doubling. Caught and reverted a bad first attempt that round-tripped the whole `locations.json` through `JSON.stringify` and reformatted every array in the file — redone as a targeted text insertion. Full detail in T21's matching bullet. |
| 9.43 | 2026-07-27 | Reconciled `THREADS-v9.20-addendum.md` against the stable registry rather than merging its obsolete IDs literally. Streaming/LOD folded into T24; World Feature Manifest narrowed into current T26 under T23's runtime/data-hybrid decision; spatial distortion extended current T27; diegetic terminal/comms opened as T29; multiplayer/server authority expanded under reserved T11 with WebSockets-before-WebRTC sequencing. Added P11 (Transform Reality, Not Assets), O12 (perceptual vs. authoritative distortion), and the executable handoff `plans/addendum-reconciliation-implementation-v1.md`. |
| 9.42 | 2026-07-27 | Building collision landed (the "buildings are a follow-up" gap from 9.41): `CompositeLocations.ts` returns a `Collider` per building, radius from each glTF's real scanned footprint. One building-medium on the boulevard is tagged `"id": "milos-building"` in `locations.json` and excluded, so it alone stays walk-through — committed separately (`845004d`). Same session, on top of that: Milo's building got real interior geometry (door, 11-step stairwell, open-topped second-floor room shell) via `buildMilosInterior`, which required pulling it out of the shared thin-instanced "building-medium" batch into its own standalone container (`loadMilosBuilding`/`placeSingleTransformMesh`) since thin instances can't carry unique per-instance geometry. Building that exposed a real gap — `PlayerController` had no concept of standing on indoor geometry, only outdoor terrain height — so `@dissonance/player` gained `FloorSurface`/`setFloorSurfaces()` (step-up-tolerant rectangle matching, see T21). Also fixed: a stale-generation race in the async `compositeLocations` rebuild, and (unverified live) a shadow-map banding/aliasing artifact on the boulevard sidewalk (`Sun.ts`: 1024→2048 map + `normalBias` added; root cause — an unbounded shadow frustum at multi-km scene scale — flagged, not fully fixed). Full detail in T21's matching bullets. |
| 9.41 | 2026-07-27 | Player collision wired up for locations.json props and utility poles — trail-viewer never called `PlayerController.setColliders` before this (Walk mode walked through everything); it turned out `@dissonance/player`'s circle-based collision (`Collider { x, z, radius }`, axis-separated slide-along-wall in `tryMove`) already existed and is already used by DTA's forest trees, just never wired here. `LocationPropsHandle`/`UtilityCorridorsHandle` now return a `colliders` array built alongside placement (`PROP_COLLISION_RADII` in `LocationProps.ts`, one eyeballed circle per prop type; `POLE_COLLISION_RADIUS_M` in `UtilityCorridors.ts`), combined and applied via a new `applyPlayerColliders()` in `main.tsx`, called on initial load and every `rebuildLocationProps` rebuild. **Deliberately scoped down, per Dan's sign-off**: buildings (`compositeLocations`) and Drive mode are follow-ups — a circle collider fits a rectangular building poorly (clips corners or blocks outside the flat walls), and only `PlayerController`/Walk has collision logic at all today (Fly/Drive have none). |
| 9.40 | 2026-07-27 | Utility corridor extended to "the end of the world" in both directions: `LocationEntry.corridor` gained `extendToWorldBounds`, `UtilityCorridors.ts` gained a ray-vs-bounding-box extension (`rayExitDistance`/`extendPathToWorldBounds`) that pushes both open ends out to the same real DEM bounding rectangle `clampToWorldBounds`/`MountainRing` already treat as the map's hard edge — not an arbitrary large number. Boulevard doubled: entire compound (26→52 placements, 9→18 buildings) duplicated at a +14.5-grid-unit offset chosen so the sidewalks tile with zero gap; the street pavement's own seam is slightly looser (~3m gap, not perfectly continuous — flagged, not yet fixed). Window-card texture (v9.38's `buildWindowCardTexture`) fixed to inset the glow gradient behind a dark margin (`WINDOW_CARD_MARGIN_FRACTION`) rather than filling edge-to-edge — the flat gradient's crisp edges were exposing UV/frame overscan the old busy photo texture used to hide, caught from a screenshot of visible light leaking past an arched window frame. Window silhouette (`ENABLE_WINDOW_SHADOW_FIGURE`) turned off per Dan's request, code left in place. Locations dropdown added to the HUD's Navigation & Views section (`GotoRow`), sourced from `locations.json`, reusing the existing Go-to teleport path. **"Door to nowhere" found and deliberately left alone** — a city-kit building instance on "dissonance boulevard" has a door with no balcony/landing reaching it; flagged by Dan as a diegetic narrative opportunity, recorded under T7's parking-lot entry so it survives a later building-geometry pass rather than getting silently fixed. |
| 9.39 | 2026-07-27 | Shipped the first real content against the T7-substrate decision (v9.38): utility poles + sagging multi-wire spans (`world/UtilityCorridors.ts`, `buildUtilityPole` in `LocationProps.ts`, a new `corridor` field on `LocationEntry`), one corridor crossing "dissonance boulevard" (`locations.json`), a "Power lines" HUD toggle correctly gated to non-orbit mode (buildings/corridors only load in that path — caught before it could throw in level 3). Full detail folded into T21's bullet list. **T28 added** — the remaining seven categories from the `@dissonance/rural-infrastructure` prompt (farm silo, acoustic windmill, water tower, self-storage, regional airport, two-lane highway, physical compass) recorded as an explicit unscoped backlog so the ideas survive even though only power lines got built — includes the finding that a physical compass isn't greenfield (`packages/navigation` already has `CompassReading`/`bearingBetween`/etc.). Confirmed via direct inspection: every prop type referenced across all of `locations.json` (rocks, trees, stone-steps, picnic-table, trash-barrel, post-grill, root-ball, giant-snag, mossy-log, dead-fountain, shed-shell, street-lamp) has a registered builder in `LocationProps.ts` — all ten non-boulevard locations are live (as placeholder primitives, per T17's asset-queue framing), nothing orphaned. |
| 9.38 | 2026-07-27 | **T7 substrate resolved (Dan's call, prompted by a `@dissonance/rural-infrastructure` feature request that would have proposed a third version of the same architecture):** the two parked data-first placement docs (`instance-placement-prompt-v1.md`, `scatter-placement-prompt-v1.md`) are marked superseded on their offline-manifest core premise — the runtime/data-hybrid approach already shipped (`ThinInstanceTrees`, `HeroTreeInstances`, `bulkForestScatter`/`trailsideScatter`, trail-viewer's `CompositeLocations`) stays the standing architecture, since it's what every session has actually built and what the live HUD-slider tuning workflow depends on. Two ideas salvaged as their own new, unscoped threads rather than adopted wholesale: **T26** (identified-overlay landmark layer — stable per-instance identity for landmarks/compass-targets/interactables) and **T27** (zone-field atmosphere blending — spatially-varying fog/grading, feeds T25's region×clock×detection compose rule). Cell-based LOD stays with T24, unaffected. Registry updated (T7/T26/T27); T23's own T7-substrate bullet rewritten with the resolution. Trail-viewer separately gained a `windowTintColor`/`windowGlow` HUD-tunable pair on `CompositeLocations`' city-kit buildings (default white/0 = today's un-tuned look unchanged), a conservative bloom pipeline, and a `MI_FakeInterior_*` window-card rework — the kit's tiled baked-photo "little room" card (repetitive/mundane at scale) replaced with a plain procedural glow-gradient card carrying the same tint/glow, plus one still silhouette (T7's "window occupancy" idea, small static cut — not the full rarity/motion spec) composited onto one building type's card. New `"dissonance boulevard concept art"` `views.json` entry captures the tuned violet-window night look this was built against. |
| 9.37 | 2026-07-24 | Wired the "Wind vol" HUD slider to foliage sway, not just wind audio — both already tracked `weatherSystem`'s live windIntensity, but only the audio callback also multiplied in `audio.windVolume.value`. Added a small `FoliageSwaySource` adapter in `main.tsx` doing the same multiply for the visual side, passed to bulk forest's and trailside's `loadHeroTreeInstances` calls in place of the raw `weatherSystem`. `WeatherSystem`/`FoliageSwayPlugin` themselves untouched. |
| 9.36 | 2026-07-24 | Fixed a bug in 9.35's bulk-forest understory mix, caught on first playtest (Dan: fallen tree floating / sitting on top of another forest object): `createBulkEligibleCandidatePositions`'s seed depended only on `radius`, so every species scattered at the same `bulkForestRadius` drew the identical deterministic point sequence — understory props landed exactly on top of the dominant tree and each other, not just nearby. Fixed with a per-species seed salt (FNV-1a hash of the asset label) so species decorrelate while staying reproducible per radius. |
| 9.35 | 2026-07-24 | Prototyped bulk-forest variety: a small weighted mix of already-shipped `tree_stump_01`/`dead_tree_trunk_02` (3%/2% of the dominant scatter tree's count) layered in as understory, same pattern `HERO_ASSETS`/trailside already use, no new HUD controls. Answers Dan's "more detail or more species" question for the tier that's now bulk forest's sole responsibility since the hero grove's removal — giving the dominant asset itself more detail remains a separate, unaddressed, competing lever. |
| 9.34 | 2026-07-24 | Removed `ThinInstanceTrees` (procedural forest-wide scatter) and the near-spawn hero grove entirely, per Dan's request — deleted `ThinInstanceTrees.ts`/`TreeCountRow.tsx`/`state/treeScale.ts`, relocated the shared `TreePoint` type from `ThinInstanceTrees.ts` to `ForestFire.ts` (its last real consumer), dropped both tiers' Dev HUD sections and the "Thin trees" visibility toggle. Bulk forest, trailside scatter (which keeps `HERO_ASSETS`' species weights), `ForestFire` (keeps `treePointsInRegion`), and `CompositeLocations` all untouched in behavior — verified via clean `tsc --noEmit` (trail-viewer, packages/world, dont-turn-around) and an in-browser reload with matching console counts. |
| 9.33 | 2026-07-24 | Wove the Lineglass geospatial-reference design into T21. The prospective device can selectively expose the existing WGS84 grid/local origin, authored landmarks, GPX routes, and (only as offline, attributed data) OSM-derived reference geometry. Explicitly planning-only: the graticule is real, but no Lineglass runtime, in-device GPX rendering, or OSM bundle exists yet. No live network or conventional minimap; geographic records remain fallible Synod/municipal evidence and fall back cleanly to grid + landmarks. |
| 9.32 | 2026-07-24 | Dan asked for a review of the Boulevard compound build method (written by a different session) before continuing. Found the architecture sound but two real bugs in 9.31's grading work: isotropic scale (buildings grew taller purely from the H-scale slider, decoupled from V-exag, unlike every sibling scatter system's separate H/V scale) and grading constants mixing rendered-space and real-meter units (correct only at hScale=1). Fixed both, plus the grading plane's fixed-15m/anchor-only sampling (now a central difference across the compound's own bounds), a material leak on rebuild, and a pad/terrain z-fighting risk. Separately, root-caused and fixed the terrain looking "striated, like a skin on a mesh": `HeightmapTerrain.ts`'s ground mesh triangulates every quad along the same diagonal (confirmed in Babylon's own `groundBuilder.js`) with mesh-derived normals — checkerboarded the diagonal and replaced normals with ones sampled from the DEM's own continuous gradient, no added triangles. |
| 9.31 | 2026-07-24 | Picked up two of the three loose ends from 9.29's Boulevard pass (terrain integration + footprint growth; corner-sidewalk/intersection-eyeball item stays open). `CompositeLocations.ts`: replaced independent per-placement `getHeightAt` sampling with one shared linear tangent-plane fit per compound (3 DEM samples at the anchor), so adjacent modules share a smooth grade instead of drifting with per-vertex DEM noise; added a procedural grade-pad mesh per compound (tilted-with-the-plane, footprint-bounds + margin, buried base) to absorb the seam against the surrounding untouched terrain — deliberately narrower than T23's still-gated general terrain-stamp system. `locations.json`: capped the cross street's dead end with a terminus building, mirrored its one-sided building/lamp row onto the north side. Verified via clean `tsc --noEmit`, matching `[CompositeLocations]` console counts, and an in-browser look via chrome-devtools (no real pointer-lock mouse-look available from CDP-issued input in this environment — verification used synthetic keyboard-driven flight, not a normal walkthrough; a manual pass is still worth doing). |
| 9.30 | 2026-07-24 | Dan asked to replace "the first DTA level environment" with the `dissonance environment test 1` trail-viewer view's data. Found it's not a config edit — audited the gap between DTA's fixed procedural `Terrain`/`ForestGenerator`/`ExperienceProfile`/`RunProfile` and trail-viewer's DEM/scale-tuning/tiered-forest/atmosphere systems (roughly two-thirds of that view's fields have no DTA-side home today); wrote `docs/plans/dta-environment-port-prompt-v1.md`, an audit-only scoping doc with five open questions gating any implementation session, framed as feeding T23's existing "DTA-onto-TrailViewer work items" rather than competing with it. Logged under T21 (origin of the `dissonance environment test 1`/`test 2` exploration) and T23 (new work-item bullet). No code changed. |
| 9.29 | 2026-07-24 | Boulevard block polish + first cross street (T-intersection, real crosswalk decal, repeated street lamps, decluttered point-props; terrain-grading gap confirmed but not yet fixed). Removed the forest-fire dev-HUD controls and readout line (system itself untouched); readout now shows player lat/lon instead. Parking lot: forest-fire-in-Dissonance and DTA-style firefighting-minigame ideas recorded. |
| 9.28 | 2026-07-23 | Dan accepted leaf-card loss for dense/non-trail forest, reopening aggressive low-poly experiments on the existing bulk `tree_small_02` tier. Added compound geographic locations (lat/long anchor + rotated local meter grid/repeats) and built the first Dissonance Boulevard block from selected CC0 Downtown City MegaKit glTF assets. |
| 9.27 | 2026-07-23 | Implemented T21 foliage wind sway: shared `@dissonance/world` `FoliageSwayPlugin`, live `WeatherSystem` intensity/time uniforms, translation-hashed thin-instance phase variance, procedural-canopy and hero leaf-card attachment, and one shared weather source across bulk/procedural/trailside/hero trail-viewer tree tiers. Trunks and dead wood remain rigid; DTA `ForestGenerator` adoption remains out of scope. |
| 9.26 | 2026-07-23 | Landmark manifest (`locations.json`) wired to a new `LocationProps.ts` — named real-world coordinates get crude primitive thin-instanced stand-ins (rocks/stone-steps/picnic-table/trash-barrel/post-grill/root-ball/giant-snag/mossy-log/dead-fountain/shed-shell) for the T22 asset-queue props that don't have authored assets yet, meant to be swapped later without touching the placement mechanism. First entry: "mountain crater" tagged `rocks`. Parking lot: scale-distortion-as-hallucination idea recorded (Synod sound-as-weapon perceptually warping the player's view of world scale via the session's own H/V-scale systems) — idea stage, not designed or built. |
| 9.25 | 2026-07-23 | Ground material reworked from v9.23's single-texture pass to a real slope-blended 3-tier `TerrainMaterial` (`forest_ground_04`/`rocky_terrain_02`/`marble_cliff_03`, blend weights baked from DEM slope into a procedural mix texture) — v9.23's `spec`/`disp`-unused gaps no longer apply (superseded, not layered under). Bulk/non-trail-adjacent forest tier added: real decimated trees now fill part of `ThinInstanceTrees`' own candidate pool instead of 100% primitive geometry, direct response to Dan's side-by-side comparison. Trailside scatter's yellow-OSM-trail extension (noted in-progress as of v9.22) confirmed via playtest and committed. Full detail under T21/T22. |
| 9.24 | 2026-07-23 | Reviewed `docs/plans/dissonance-camera-aware-foliage-prompt.md` (session) — scoped down to wind sway only; corridor/dissolve/push/Synod pieces shelved (no alpha-tested foliage in `ForestGenerator`, no third-person camera or `apps/dissonance-*` workspace to attach them to). Found and flagged (not silently fixed) a phantom-doc contradiction missed by v9.22: `FoliageSwayPlugin`, and separately three-zone LOD, have been cited as already existing since v9.7/v9.20 (T22, T24) but neither exists in code — confirmed against `generation-systems-audit.md` and a repo-wide search. Real reusable substrate identified: `WeatherSystem`'s existing `windIntensity` gust simulation, already ported to trail-viewer and left running every frame "for future consumers" (T21, v9.19) — this is that consumer. Plan recorded under T21: a `MaterialPluginBase`-based `FoliageSwayPlugin` driven by `windIntensity` + a shared time accumulator, per-instance phase hashed from thin-instance world-matrix translation (no new attribute buffer), attached only to `ThinInstanceTrees`' canopy materials and `HeroTreeInstances`' leaf-card materials (covers bulk-forest/trailside/hero-zone tiers in one plugin). Not yet implemented. |
| 9.23 | 2026-07-22 | `HeightmapTerrain` gained an optional textured ground material (Poly Haven `rocky_terrain_02`, CC0) — tiled PBR albedo/normal/roughness in place of the flat elevation-gradient vertex colors, mutually exclusive with that gradient rather than blended. ffmpeg's EXR decoder failed silently on the roughness map's Gray DWA compression (all-zero output, no error at all) — Blender 5.2 (installed, not on PATH) used instead via headless Python, colorspace-verified by comparing raw pixel values before/after. Three known gaps logged, not closed: dead "Terrain (low/high)" HUD color picker, unused `spec` map, unused `disp` map. Full detail under T21. |
| 9.22 | 2026-07-22 | Full docs/-directory read-and-reconcile pass (Dan request). Six previously-uncommitted-to-this-file `docs/plans/*.md` docs folded in: `creature-silhouette-direction-v1.md` (new design canon under T4, paired with `animals.webp`; authoring rules cross-referenced under T17 — landed under T4/T17 rather than the doc's own "T8 addendum" framing, same correction pattern as v9.20's hero-tree fold-in, since T8 is retired) — `geo-grid-engineering-prompt.md` (its spec shipped: lat/long graticule overlay, closing NOTES.md item 2, logged under T21 + parking lot) — `instance-placement-prompt-v1.md` + `scatter-placement-prompt-v1.md` (surfaced as a real design/implementation gap under T23: both describe an unbuilt manifest-driven placement+zone-field architecture that contradicts the runtime-procedural-scatter code that actually shipped; not resolved, flagged for Dan's call) — `sound-as-control-prompt-v1.md` (T9's actual handoff doc, supersedes the Godot oscilloscope reference; narrowed T9's gate to the specific `@dissonance/audio` bus/analyser surface T3 Phase C would provide, not "T3" loosely) — `t13-boulevard-extraction-v1.md` (T13 updated from "parked pending capture" to "stills + analysis landed, video pass outstanding"; new seeds folded in: waveform locomotion → T4/T9, break-and-hack → T4, wellbeing HUD → new unrouted experimental parking-lot item, canon-risk register vs. T10's isometric rule). Found a byte-identical duplicate of the T13 doc (`t13-boulevard-extraction-v1 (1).md`) and deleted it (Dan confirmed). A new **Doc inventory** section lists every "owning doc" this file names that isn't actually a committed file (`dta-session-notes.md`, `acoustic-world-systems-prompt-v1.md`, `pursuer-embodiment-prompt-v1.md`, and others), a pattern previously known only for T3's v3 prompt. Also logged undocumented-until-now shipped work found by reading source directly: the T22 hero-tree POC landed and exceeded its own spec (5 real Poly Haven `.glb` assets, a solved multi-material thin-instancing gotcha) with an in-progress uncommitted extension (trailside scatter onto yellow-blazed OSM trails, not just the GPX track); a stuck-movement-key-on-blur fix across all three player controllers; `WaterPlane`'s bump-texture now optional. Execution-plan T13 row updated to match. |
| 9.21 | 2026-07-21 | Fixed the trail-viewer Mode `<select>` (Walk/Fly/Drive) bug Dan reported as "still broken" after a prior session's blur()-on-change fix (real, but not the cause). Root cause: `MovementRow.tsx`'s onChange wrote the new mode into the shared `activeMode` signal *before* calling `switchMode` (`main.tsx`), so `switchMode`'s own no-op guard saw the value already changed and skipped the real handoff (position, ground-snap, rotation, `scene.activeCamera`) — dropdown/readout relabeled but the camera never switched. Fix: let `switchMode` own the signal write, as it already did. Full detail under T21. |
| 9.20 | 2026-07-21 | Hero/scatter tree authoring tier added to T22: bent-normal leaf cards via Blender Data Transfer modifier, interior card fill for hero-tier only, one authoring template → two output tiers via decimation; confirmed compatible with thin instancing/T24's three-zone LOD/`FoliageSwayPlugin`. POC step precedes original authoring — source one Poly Haven CC0 reference tree to validate the Blender→glb→Babylon round trip before Dan builds an original template; Quaternius CC0 pack usable as placeholder scatter-tier density in trail-viewer meanwhile. Owning doc: `hero-tree-poc-prompt-v1.md`. **Contradiction found and resolved (not silently):** the source fold-in note (`docs/archive/THREADS-fold-in.260721.md`) targeted "T8" with rigging framing — T8 is retired/merged into T17 per the v9.3 registry, its anchor text no longer exists, and the content itself (static foliage authoring, no skeleton) doesn't fit T17's rigging-ladder scope anyway. Landed under T22 instead, confirmed with Dan rather than assumed. |
| 9.19 | 2026-07-17 | Large trail-viewer session, same day as 9.18: (1) `views.json` relocated `docs/` → `public/data/`, static import → runtime fetch (9.18's "no relocation needed" note corrected). (2) HUD redesign — toggles consolidated into one grid (was the main complaint), sliders regrouped semantically (World vs. Sky, tree count moved out of Atmosphere), panel made scrollable, copyable-lat/lon control added (`worldToLatLon` new in `packages/geo`), full-screen loading overlay added. (3) DTA `WeatherSystem` ported as-is (already fully decoupled) — Windy toggle, wind-driven cloud drift. (4) DTA player/audio systems audited and selectively ported: `BreathSystem` surfaced (was already running, unread); `AmbientAudio`/`HeartbeatAudio` reused as-is; new `TrailPlayerAudio` decoupled sibling of `PlayerAudio` + new `AudioEngine.playTrailStep()`; `AdrenalineSystem`/`RiverAudio`/`DestinationAudio` deliberately not ported (no driving signal / DTA-specific content) with reasons recorded, not silently dropped; new "🔊 Enable Audio" gesture button (trail-viewer had no prior user-gesture point for AudioContext unlock). All new settings persisted through `SavedSettings`/autosave/Copy View per Dan's "persist all environment values" instruction. Full detail under T21. |
| 9.18 | 2026-07-17 | Saved-views dropdown added to `ViewToolsRow` (both orbit and player mounts), reading `docs/views.json` via a static import; shares Load View's apply path through an extracted `applySnapshot` helper. Dan added a 4th views.json entry, `dissonance-forest-scale`, as his candidate scale for the DTA/Dissonance port. Verified via Playwright in both camera modes. |
| 9.17 | 2026-07-17 | `docs/NOTES.md` and `apps/trail-viewer/docs/views.md` added to the repo (Dan). Views archive formalized as `apps/trail-viewer/docs/views.json` — a real JSON array of `{ name, ...ViewSnapshot }` objects, replacing the old name-line-plus-markdown-block format under T21; existing 3 entries migrated verbatim via parse script, not hand-transcribed. NOTES.md's two untriaged items (Mac vertical-elevation bug, lat/long overlay feature request for the landmark manifest) logged to the parking lot under T21 rather than left undiscoverable in a separate file. |
| 9.16 | 2026-07-17 | T3 Phase 0 audit run (local session, `pursuer-extraction-prompt-v3.md`, pasted not committed). Found Phases A (PursuerSystem) and B (HeartbeatGlow split) already fully landed, not recently — T3's "ready to run"/blocker status was stale. Phase C (PursuerAudio → `@dissonance/audio`) deliberately left undone: its own `// EXTRACTION CANDIDATE` comment argues against extracting with only one consumer, and Dan confirmed honoring that over the handoff prompt's literal instruction (P1/CLAUDE.md's "don't over-extract," restated not reopened). Two contradictions surfaced, not resolved: (1) this doc and the extraction prompts use `@dta/*` throughout but no such scope exists — everything is `@dissonance/*`; (2) `HeartbeatGlow`'s comment claims BPM sync with `HeartbeatAudio` that doesn't currently exist (`setStressLevel` is a no-op, bpm hardcoded at 65). Added **O10** (does T4 actually need Phase C, or is A/B enough?) and **O11** (does O8's audio-single-writer queue still apply, given Phase C isn't happening on T3's current trajectory?) — both cheap, both potentially unblock downstream work immediately. T4 and execution-plan T3/T18/T19/T20-Phases-2+ rows annotated to point at O10/O11 instead of asserting still-blocked. |
| 9.15 | 2026-07-15 | Execution-plan "Ready now" table now carries T24's serial-with-T21 concurrency advisory inline on both rows (previously only in T24's own full entry — the table itself gave no warning). |
| 9.14 | 2026-07-15 | Contradiction-pass fixes (Dan): flashlight correction — T24's "T15/P5 forbid flashlight" was wrong (P5 is firefly-color exclusivity only); current `dont-turn-around` flashlight system stays in both the live app and its museum-piece copy, flagged as a reuse candidate for DTA-onto-TrailViewer work. T21 gained a **Downstream** line backlinking T24/T25/landmark-manifest (previously only discoverable by reading those threads). Execution-plan header's stale "v9.5 snapshot" label corrected to note it was last touched at v9.7. |
| 9.13 | 2026-07-15 | T25 atmosphere grading + time-of-day added (side-session spec, placed as new thread): dawn look as post-stack data via T1 schema extension; ColorCurves-over-LUT for interpolation with LUT both/and for fixed-mood interiors (mutually exclusive per profile, loud validation); three-driver transition seam named (region × clock × detection — build once); compose rule proposed as O9; trail-viewer’s existing setTimeOfDay noted as prior art (P1). |
| 9.12 | 2026-07-15 | Shed/comfort station added (photo 10): smallest full interior — completes the acoustic ladder pavilion→shed→true interiors; dead-plumbing sibling to the fountain; graffiti = first trace of others; one-exit shelter/trap lever left open. Shell joins T17. Photo set 1–10 named the “SMR starter pack.” |
| 9.11 | 2026-07-15 | Signal-coverage-as-geography lore layer added to T10 (provisional; DTA signal=safety / Dissonance signal=surveillance — T14 one-mechanic-two-fictions; NO SERVICE phone-recovery beat; coverage as a T23 region-mask channel; phone-glow vs P5 flagged open). Giant snag (photo 8) + open grove zone type (photo 9, fourth hub-gradient band) added; snag joins T17. Landmark manifest proposal (`landmarks.geojson` in T21 pipeline); EXIF stripped on upload — coordinates via originals or Photos app. |
| 9.10 | 2026-07-15 | Picnic grounds feature added to T22 (photos 6–7): stone tables as sole cover in an exposure zone; metal grills/barrels as loudest T18 surface class — distraction terrain co-located with maximum exposure; pavilion as gentlest roofed-exterior acoustic test; hub front-porch adjacency (T16, placeable via T21). Dead fountain prop added (material-conditions storytelling, candidate anomaly seed). Picnic kit joins T17 queue. |
| 9.9 | 2026-07-15 | Random stairway feature added to T22 (photo 5): traversal + acoustic exposure (rhythmic stone cadence = most classifiable gait signal) + axial sightline corridor; never explained per diegetic-discovery filter; modular thin-instanced step unit (joins T17); heightmap-snapped, not a stamp; SMR real staircase locations placeable via T21 pipeline. |
| 9.8 | 2026-07-15 | T24/T21 concurrency advisory recorded (serial default; forest-module fence if concurrent) in both THREADS and the owning prompt. Root ball upgraded to interactable: loose pushable limbs (Dan field note) — player-caused motion only, damped hinge (no physics engine), noise event via T18 grammar; dual role as distraction tool and brush-noise hazard; limb rig joins T17 queue. |
| 9.7 | 2026-07-15 | T24 forest performance & chunking added (Dan’s ChatGPT prompt conformed: Phase 0 audit, trail-viewer target, T22-neutral aesthetics, profile-ready tunables, T23-compatible chunk metadata); owning doc `forest-performance-prompt-v1.md` written. Creek corridor play scenario added to T22 (culvert generalized to a line; stepping-stone crossings; cut bank as first terrain stamp). Reference photo set logged (canopy, creek, fern colony, root ball); root ball + creek rocks + mossy log join T17 queue. T24 added to execution-plan ready table. |
| 9.6 | 2026-07-15 | Execution plan section added: critical path (T3→T4→T23→T5), ready/caveat/blocked tables, Dan-only decision gates, suggested this-week sequence. Snapshot — regenerate when T3 lands. |
| 9.5 | 2026-07-15 | Merged local trail-viewer session per discrepancy report (local commit `ecc0fd4` predated the web overwrite; re-merged per single-writer rule). P8 corrected — `dont-turn-around` app is the museum piece, trail-viewer is the successor foundation (Dan, direct correction). T21 expanded: Drive mode, WaterPlane, Sun/StarField day–night, fog color, overcast, ThinInstanceTrees (8000 cap), ForestFire, Bounded World, per-level persistence; ViewSnapshot Copy/Load spec + beforeunload/autosave race gotcha (net-new); fast-travel-as-unlock intent; ski/firefight/climb generative insight (→ T6/T23). O8 reworded to O5 pattern (decided, restated for sign-off). T22 stale world-population reference → T23. |
| 9.4 | 2026-07-14 | T23 region system / continuous world added (environments as profile-selected regions in one world; shared transition seam with T5). O2 upgraded to decided-required. T10 continuous-world canon guard added. Spiral collectible placement rule added to T15/T16 (deterministic, trail-snapped, provisional). T5/T6/T7 registry states updated. |
| 9.3 | 2026-07-14 | One-time ID reconciliation. Registry table added; every section/bullet tagged; T1–T17 lineage adopted (T3/T12/T-Lola/T17 precedent honored); new IDs T18–T22; T5/T6/T7/T11/T14 reserved for parked threads; T8 formally retired into T17. IDs are now stable — future threads take T23+. |
| 9.2 | 2026-07-14 | Rigging track upgraded to Dan's authored T17 (four tiers with per-tier feeds, tier-1 failure modes, load-bearing naming convention, Lola one-rig-two-presentations rule). v8 asset queue preserved inside T17; T4/T8 numbering note added pending repo reconciliation. |
| 9.1 | 2026-07-14 | Gap fix: dta-session-notes §1–4 (palette, culvert/mow-line scenarios, concealment-mask/stamp/foliage engineering, FPPOV lighting) were orphaned during the v8 structure adoption — now indexed as a compact active-thread entry; lore pointer broadened. |
| 9 | 2026-07-14 | Wove `acoustic-world-systems-prompt-v1.md` (DDI spine). New master thread (8-layer separation, typed acoustic domain, voice rig/vocoder, mech-dog challenge slice, phases 0–7). Sound-as-control superseded by voice rig (oscilloscope → museum). SensorProfile 4th axis + PerceptualLODProfile on embodiment. Wildlife extension path + specific-wrongness roster. Lore: disarmament contradiction, ambiguity of compliance, sensory punishment, Lola mechanical role. P9 baseline-first, P10 audible≠harmful. O6 bus naming, O7 sensor axis schema, O8 sequencing. Rooms as diegetic processors; acoustic persistence roster. |
| 8 | 2026-07-14 | Reconciled divergent v7s. Restored T12 (was cited by D1 but missing). Added: dog ambiguity ladder (embodiment), asset-queue additions (live dog pair rule, player rig, vultures, stagger clip), route-viz HUD candidate, Godot excavation next-action (stills + 3 timings), isometric camera rule package (O4), O5 persistence sign-off, single-writer conflict rule. Parking lot enriched (false-scare details, window occupancy, RoomProfile). Viewpoint binding flagged narratively load-bearing. |
| 7 | 2026-07-14 | Consolidation. Throw & distraction thread rewritten nonviolent (D2 frozen); melee prompt superseded. D1/D1a recorded. T-Lola addendum, DTA spine, hub structure, playable archive (P8) folded in. Parking lot + open decisions O1–O3 refreshed. |
| 6 | prior | Baseline for this consolidation. |
